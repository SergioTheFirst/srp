# CONTINUITY.md — Session Continuity Ledger

_Canonical briefing. Survives compaction. Facts only; mark `UNCONFIRMED` if unsure._

## Goal (incl. success criteria)
- **АКТИВНЫЙ ТРЕД (2026-06-24): NETMAP-UNIFICATION (план)** — объединить «Топология» (netdisco UI: `/topology`+`net_device.html`+`server/netdisco/`) и «Карта сети» (`/netmap`+`server/analytics/netmap.py`) в ЕДИНУЮ «Карту сети». Анализ завершён 5 параллельными Explore-агентами + верификация (db.py:200-301, dashboard.py:278-417). **Вывод:** netdisco-граф = персистентный ХРЕБЕТ (net_devices/net_links/snapshots/changes/reachability), netmap = эфемерные ОВЕРЛЕИ (качество/субсеть-аномалия/кластеры/анимации) — НЕ вторая модель. Физ.устройство = до 3 НЕсвязанных записей (devices/net_devices/printers); универсальный ключ = нормализованный MAC, нигде не персистится. **Решения владельца 2026-06-24:** идентичность = FK-связка (НЕ слияние таблиц); +панель управления картой («скрыть ARP-only» и др.); цель = гениальная интерактивная карта; собирать данные с активного оборудования любым безопасным агентлесс-способом. **План v2 (10 фаз, R4):** `docs/superpowers/plans/2026-06-24-network-map-unification-plan.md` — Ф1 MAC-хребет (+device_id/printer_id FK, аддитивно) → Ф2 ассемблер `netdisco/unified.py` (netmap→helpers) → Ф3 API+GraphCache → Ф4 один canvas `_netgraph.html` → **Ф5 панель управления (фильтры/слои/кнопки/боковая-панель/путь/первопричина/машина-времени по net_topology_snapshots/экспорт)** → Ф6 каноническая карточка (card_url agent→printer→infra) → **Ф7 Tier-1 SNMP-углубление (LLDP port-id/mgmt-addr, dot1q VLAN-FDB, ifXTable, LLDP-MED, ENTITY, dot1dStp, PoE, HOST-RES, ipNetToPhysical, РЕАЛЬНЫЕ wireless-рёбра client→AP AIRESPACE/Aruba/MikroTik)** → **Ф8 Tier-2 пассив (reverse-DNS/mDNS/SSDP/NetBIOS/WSD/TLS-баннер → де-аноним «Неизвестного»)** → **Ф9 Tier-3 опц-адаптеры (NetworkAdapter ABC + merge-by-MAC + MikroTik/UniFi/NetFlow/Redfish; DNA/Meraki/OPNsense documented; creds via DPAPI)** → Ф10 демонтаж «Топологии». Research: 2 агента (G1/G2/G3-каталог в плане). Контракт агента НЕ тронут; security-review обяз. на Ф1/7/8/9; p0f-passive SKIP (Win raw-socket); NetFlow-dep = решение владельца. **СТАТУС: план v2 готов; РЕАЛИЗАЦИЯ НАЧАТА.** **Ф1 (MAC-FK хребет идентичности) ЗАВЕРШЕНА — branch `feat/netmap-p1-identity`, gate green cov 92.84%, smoke OK, Opus security-review=APPROVE-WITH-NITS (0 crit, 0 false-link/data-loss; H1 resilience-try/except + M1 приватный-IP-гейт + M2 nd-unknown-guard + L2 .get применены):** `netmap.agent_mac_index` (публичный единый индекс agent-MAC, дубль `inventory._agent_macs` удалён); `identity.link_identities` (join по `oui.normalize_mac` обе стороны, IP-резерв ТОЛЬКО для MAC-less строк и ТОЛЬКО приватный через `_canon_ip`+is_private, no-false-positive, nd-unknown пропуск); `db.net_devices +device_id/+printer_id` (аддитивно nullable, идемпотентная legacy-миграция, индексы `idx_netdev_mac`/`idx_netdev_device_id` ПОСЛЕ миграции колонок); `set_net_device_links` (COALESCE-preserve, без флаппинга)+`get_net_device_links`; связка в конце `run_inventory_cycle` под `_poll_lock`, best-effort try/except+log. **Cleanup-инвариант:** `net_devices` НЕ в `_DEVICE_TABLES` (узел keyed-by-MAC, не agent-owned) → на delete агента/принтера soft-FK ОБНУЛЯЕТСЯ (`device_id`/`printer_id`=NULL), узел остаётся, нет висячих указателей (`[[device-identity-cleanup-not-continuity]]`); guard-тест обновлён (`_FK_LINK_TABLES`) + позитивные NULL-clear тесты. Контракт агента не тронут (нет bump). Тесты: `test_netmap_identity.py`(10)+scheduler-link(1)+cleanup-null(1)+printer-null(1). Память `[[netmap-identity-spine]]`. **Ф2 (единый ассемблер read-side) ЗАВЕРШЕНА — branch `feat/netmap-p2-unified`, gate green cov 92.76% (1293 passed), smoke OK, Sonnet code-review REQUEST-CHANGES→исправлено (0 crit):** T1 `netmap.py`→3 чистых хелпера `subnet_hint(ip)→Optional` (None для не-IPv4; `dashboard._printer_subnet` удалён→общий), `quality_overlay(snap,gw)`, `subnet_anomaly(losses)` (извлечены из `_finalize`; `build_netmap` БАЙТ-ИДЕНТИЧЕН, верифиц.ревью). T3/T4 новый ЧИСТЫЙ `netdisco/unified.py::build_network_map(net_devices,net_links,snapshots,printers)`→суперсет `{nodes,links,subnets,totals}` (надмножество `reconcile._graph`): узлы net_devices+синтез агентов/принтеров/шлюзов, дедуп по `device_nid` (FK by_device_id/by_printer_id Ф1 → by_mac → by_ip → mint), ОДНО устройство=ОДИН узел; `nd-unknown` НЕ размещается (как `link_identities`); узел несёт dev_type/ip/mac/vendor/status/subnet/subtype/FK/`card_url`(agent>printer>net)/provenance; рёбра net_links(+стаб для висячих, gateway-стаб→router)+agent-uplink с quality; `medium`=l3/wireless(линк→AP Ф2-эвристика, Ф7 уточнит)/wired; субсеть-аномалия=поле. Исправления ревью: HIGH nd-unknown-коллизия (skip-гард оба merge) + MEDIUM stub-gateway-upgrade + 2 регресс-теста. Контракт агента не тронут (нет API/UI). Тесты `test_netmap_unified.py`(12)+3 хелпера. Память `[[netmap-unified-assembler]]`. **Ф3 (единый API + GraphCache) ЗАВЕРШЕНА — branch `feat/netmap-p3-unified-api`, merge `9c6ae9f` (no-ff), ЗАПУШЕНО origin/main; gate green cov 92.56% (1301 passed), ruff/mypy/bandit clean, smoke OK; Sonnet code-review=APPROVE-WITH-FIXES (0 crit/high, все LOW/MEDIUM применены).** Новый канонический `GET /api/v1/network-map/graph` → единый граф `{nodes,links,subnets,totals}` через `GraphCache`; новый loader `netdisco/cache.py::load_network_map()` (ДЕФОЛТНЫЙ loader кэша; DB fan-out get_net_devices/get_net_links/get_network_snapshots/get_printers→`build_network_map`, loader НИКОГДА не None→`get()` отдаёт сам граф, не `{graph,received_at}`). `/api/v1/netmap`+`/api/v1/topology/graph` → **deprecated-АЛИАСЫ** (тот же `_network_map_graph` → СТРУКТУРНО один кэш-объект → идентичный граф; ФОРМА ОТЛИЧАЕТСЯ от старой: `nodes/links/subnets/totals`, НЕТ `graph`-обёртки, НЕТ `received_at`). `/discovery/poll`+`/topology/poll` инвалидируют кэш ПОСЛЕ работы (`_invalidate_network_map_cache`); фоновые циклы НЕ инвалидируют (TTL 45с поглощает steady-state). `create_app` (main.py) создаёт `app.state.network_map_cache=GraphCache()` up-front → P11-LOW ленивый init в хэндлере снят (fallback `getattr(...) or GraphCache()` только для app вне create_app). Веб `/netmap`(build_netmap)+`/topology`(get_latest_topology_snapshot) НЕ тронуты → перешли на единый граф в Ф4. Безопасность: API=application/json (`</script>` в hostname инертен), кэш не отравляется request-вводом (граф целиком из БД), нет секретов/community/публичных-IP. Тесты `test_netmap_unified_api.py`(8: форма/empty/кэш-no-rebuild-в-TTL/reload-после-TTL/алиасы=тот-граф/invalidate-after-оба-poll/XSS-inert)+обновлённые `test_netdisco_api.py`/`test_netmap_web.py`. Память `[[netmap-unified-api]]`. **Ф4 (единый canvas) ЗАВЕРШЕНА — branch `feat/netmap-p4-unified-canvas`, merge `d035fac` (no-ff), ЗАПУШЕНО origin/main; gate green cov 92.80% (1306 passed), ruff/mypy(107)/bandit clean, smoke OK; Sonnet code-review=APPROVE-WITH-FIXES (0 crit/high; XSS-invariants PASS; 2 LOW применены).** Один canvas `web/templates/_netgraph.html` (на базе `topology.html`) рисует ОБЪЕДИНЁННЫЙ граф Ф2 через Ф3 GraphCache: физика/зум/пан/пин/поиск/3-темы/`textContent`-tooltip + кольца подсетей/цвет-качество/пульс-пакета/кольца-аномалии/пунктир wireless+ambiguous; глифы ВСЕХ типов (router/switch/ap/agent/printer/server/phone/endpoint/unknown), цвет по статусу (агент — по потерям); клик → `card_url` (agent>printer>net). `netmap.html` — обёртка (toolbar+SSR-инвентарь+журнал+кнопка «собрать карту»); `/netmap` отдаёт единый граф через `_unified_map_graph` (тот же кэш); `build_netmap` ушёл со страницы (остался в `analytics/netmap.py` для `_attach_printers_to_netmap`/тестов). Симметричный glyph-апгрейд printer_id+unknown→printer. XSS: 0 innerHTML, `|tojson` остров, card_url из ассемблера. Тесты `test_netmap_web.py`(13, переписаны под unified-форму). Память `[[netmap-unification]]`. **Ф5 (панель управления) ЗАВЕРШЕНА — branch `feat/netmap-p5-control-panel`; gate green cov 92.82% (1322 passed), ruff/mypy(107)/bandit clean, smoke OK; Sonnet code-review REQUEST-CHANGES→исправлено (CRITICAL pathBfs infinite-loop + HIGH plaque divergence + MEDIUM navCache + 6 LOW), Opus security APPROVE-WITH-NITS (0 crit/high; XSS PASS).** `[[netmap-control-panel]]`. **Ф6 (единая каноническая карточка) ЗАВЕРШЕНА — branch `feat/netmap-p6-canonical-card`; gate green cov 92.94% (1334 passed), ruff/mypy(107)/bandit clean, smoke OK; Opus security-review=APPROVE-WITH-NITS (0 crit/high; SQL-{col}=compile-time литерал безопасен; redirect не open-redirect/без-петли=FK из БД+один-направленный; XSS PASS; LOW `|urlencode` на href применён).** T1 `_card_url` (резолвер agent→printer→net-infra) уже был из Ф2 — не тронут. НОВОЕ: `db.get_linked_net_device(*, device_id=None, printer_id=None)` — обратный FK-lookup (keyed, no-scan; `{col}` из 2 литералов, val биндится; `#nosec B608`)→полная карточка net-устройства (interfaces+links). `/netdisco/device/{nid}` связанного узла → **302 redirect** на каноническую (`device_id`→`/device/{id}`, `printer_id`→`/printers/{id}`) ПЕРЕД рендером; чистая инфра (без twin) рендерит net-карточку как раньше (петли нет: `/device`/`/printers` назад не редиректят). НОВЫЙ partial `web/templates/_net_section.html` (сворачиваемая `<details>` секция «Сеть — топология»: статус/тип/вендор + meta + таблица интерфейсов + список связей-соседей + ссылка на полную сетевую карточку; `net_status_ru`/`net_type_ru`/`net_conf_color` — Jinja-глобалы; href через `|urlencode`) включён в `device.html` (после агентской секции «Сеть») и `printer_detail.html`. Routes `device`/`printer_card` пробрасывают `nd=get_linked_net_device(...)`. `net_device.html` НЕ тронут (полная standalone-карточка инфры). Тесты `test_netmap_card_canonical.py`(12: приоритет card_url, reverse-FK-lookup, 302-на-агента/принтера, standalone-инфра-рендерит, секция в device/printer, нет-секции-без-связки). Память `[[netmap-unification]]` (+канон-карточка). **Ф7 (Tier-1 углубление SNMP) ЗАВЕРШЕНА — branch `feat/netmap-p7-snmp-deepen`; gate green cov 92.97%, ruff/mypy(108)/bandit clean, smoke OK; Opus security-review=APPROVE (0 crit; HIGH×2+MEDIUM×2+LOW×2 — ВСЕ применены).** Данные-слой: `net_links +a_port/b_port/medium/vlan`, `net_devices +subtype`, `net_interfaces +if_alias` (аддитивная идемпотентная миграция; upsert/replace COALESCE-preserve портов/medium, latest-wins vlan, canonical a≤b свопает И порты). **T1** directed port-labels: `collect_lldp` читает lldpRemPortId + `lldp_loc_port_ifnames`(lldpLocPortDesc) → a_port(локальный)/b_port(удалённый); fusion тащит порты со свопом при канонизации, medium (winner или 'wired'), vlan (из ЛЮБОЙ улики). **T4** dot1q VLAN-FDB: `read_fdb_dot1q` (vlan.mac6→port) → каждое FDB-ребро тегируется VLAN; `collect_evidence` предпочитает dot1q, fallback dot1d (чинит VLAN-aware свитчи). **T5** ifXTable: `ifName`(предпочт.)/`ifAlias`(cap 64) в интерфейсы. **T6** wireless: НОВЫЙ `netdisco/wireless.py::collect_wireless` — vendor-gated по sysObjectID (AIRESPACE/Aruba/MikroTik, строгая dot-граница, fail-closed) → джойн client-mac↔ap-mac столбцов по индексу → рёбра client→AP `medium=wireless` (`SOURCE_WIRELESS` приоритет 5); проброшено в топ-цикл (`collect_wireless_fn`, cap 2048/столбец). **T3+ENTITY**: `entPhysicalModelName`→точная модель (printable-filter, UNKNOWN-last порядок), LLDP-MED класс→subtype phone/ap/server — пишется ТОЛЬКО на УЖЕ-известный nid соседа (guard `known_nids`, без фантомных строк) через `_enrich_med_subtypes`+`fusion.node_id`(публичный). unified read-side: `_real_links` берёт хранимые medium/vlan/a_port/b_port (Ф2-эвристика=fallback), узел несёт хранимый subtype; canvas `_netgraph.html` показывает VLAN в тултипе (порты/medium/wireless-пунктир уже были). Security-fix: `collect_lldp_mgmt` (T2-seed, ПОКА не подключён) теперь RFC1918-гейтит mgmt-IP (fail-safe до подключения) + переименован misleading-constant. **Carry-forward (НЕ блок, документировано):** T2 lldp-mgmt seed НЕ подключён в discovery (collector готов+тестир.+gated — подключать с RFC1918-гейтом уже внутри); STP role-направление аплинка, PoE-подтверждение, hrSWRun server-роль, IPv6-ND harvest — отложены (UNKNOWN-over-false-confidence / сложность). Тесты `test_netdisco_*_p7.py`: db(15)/evidence(13)/fusion(4)/unified(5)/probe(9)/wireless(5)/reconcile(3). Память `[[netmap-unification]]`. **Ф8 (Tier-2 пассивная де-анонимизация — «убрать Неизвестное») ЗАВЕРШЕНА — branch `feat/netmap-p8-passive`; gate green cov 91.94% (полный сьют pass), ruff/mypy(111)/bandit clean, smoke OK; Opus security-review=APPROVE-WITH-NITS (0 crit/high; 7/7 инвариантов PASS — SSRF/RFC1918-гейт, fail-closed-парс, fill-empty-only, caps/lock, sink-санитизация, secrets, secure-default; 2 LOW применены).** Всё пассивное/offline = САМЫЙ НИЗКИЙ приоритет (заполняет только ПУСТОЕ поле, НИКОГДА не перетирает agent/SNMP). **Данные-слой:** `db.fill_net_device_identity(nid,*,hostname/subtype/model)` — зеркало upsert-COALESCE: тут ХРАНИМОЕ выигрывает (`COALESCE(net_devices.col, ?)`), UPDATE-only (пассив НЕ создаёт узел; неизвестный nid / all-None = no-op; параметризован). **Чистые парсеры+коллекторы (инъектируемый сокет, fail-closed «мусор→None», RFC1918/link-local-гейт по SOURCE, caps):** `netdisco/passive.py` (T3 mDNS/DNS-SD 5353 +QU-бит, T4 SSDP/UPnP 1900, T5 NetBIOS-137 + WS-Discovery 3702; мультикаст egress TTL-1 → не покидает сегмент; `_read_name` ограничивает compression-hops=32 → нет петли; имена mDNS/NetBIOS через `_clean_host` allow-list), `netdisco/naming.py` (T2 reverse-DNS `gethostbyaddr` пул+кэш(pos+neg)+RFC1918+cap+overall-deadline), `netdisco/banner.py` (T6 АКТИВНЫЙ: TLS `getpeercert` CN/SAN + HTTP `Server` на 443/80; no-redirect-opener=SSRF-safe; chain-validation ON → LAN self-signed fail-closed к None, HTTP-баннер несёт идентификацию). **Оркестратор `scheduler.run_passive_cycle(cfg)`:** гейт `enabled AND passive_enabled`, под `_poll_lock`; by_ip=ВСЕ private-узлы (мультикаст бьёт по любому), targets=безымянные (`_passive_target`: нет hostname или dev_type unknown/endpoint), unicast/reverse-DNS гонят только targets (cap 1024); источники по `cfg.passive_protocols`; T1 `_deanon_from_data` (IP из `iter_printer_port_map` → subtype=printer, offline); merge IP→nid ПО-ПОЛЬНЫМ приоритетом (hostname: netbios>mdns>reverse_dns>banner · subtype: data>ssdp>wsd>mdns>netbios · model: ssdp>banner); responder вне инвентаря ИГНОРИРУЕТСЯ (узел не создаётся); banner жёстко ограничен (`_BANNER_CAP=32`×`1.0с`, 2 touch/host → ≤~64с под локом, чтоб не голодали др. циклы). **Конфиг:** `passive_enabled`/`passive_interval_sec`(clamp floor 60)/`passive_protocols`(allow-list 7 имён, empty→all) в `NetdiscoConfig` (OFF-by-default); **ВКЛЮЧЕНО в shipped `server/config.json`** (no-disabled-by-default). **Цикл `main.py` `_run_netdisco_passive_cycle`+`_netdisco_passive_loop`** (double-gated netdisco+passive, self-guard try/except, interval+jitter). `unified.py` НЕ тронут — ассемблер уже отдаёт hostname/subtype → де-аноним виден на карте автоматически. **Carry-forward (НЕ блок):** (a) T6 TLS даёт имя только для CA-валидной цепочки (LAN self-signed → пусто, честно; HTTP `Server` несёт баннер); (b) провенанс «reverse-DNS/passive» как фильтр Ф5 НЕ добавлен — нет колонки источника имени в `net_devices` (отдельная аддитивная задача, если владелец захочет фильтр по источнику). Тесты `test_netdisco_*_p8.py`: passive(11)+naming(6)+banner(8)+passive_wire(9: writer+оркестратор+T1)+config(5). Память `[[netmap-unification]]`. **Следующая = Ф9 (Tier-3 опц-адаптеры: `NetworkAdapter` ABC + `adapter_merge` by-MAC + MikroTik REST/UniFi/NetFlow-IPFIX/Redfish; creds via DPAPI; `netflow` PyPI одобрен владельцем, изолирован) — R4 + security-review ОБЯЗ.; затем Ф10 демонтаж «Топологии».**
- **✅ ЗАВЕРШЁННЫЙ ТРЕД (2026-06-23): PRINTVIEW** — полная переработка вкладки «Печать» по плану `printview.md` (корень репо, 12 фаз). Источник истины = `print_jobs` (связка компьютер→принтер→страницы); IP принтера резолвится НА СЕРВЕРЕ из `historical.printer_ports` (агент/`shared/schema.py` НЕ трогаем). Решения владельца (2026-06-23): экспорт ТОЛЬКО CSV (без новых зависимостей; Excel/PDF — печать браузера), графики «пользователи/отделы» ОСТАВИТЬ (аддитивно), counter-строки в таблице помечать «оценка»; **per-phase отметки в CONTINUITY + авто merge/push без напоминаний.** Память: `[[printview-ip-resolution]]`. **P1 СМЁРЖЕН+ЗАПУШЕН в origin/main (`a050d7d`, 2026-06-23): новая служебная `printer_ip_map(device_id,printer,ip,updated_at)` PK(device_id,printer) + idx_pim_ip; апсерт `store_printer_ip_hints` на каждый `historical`-ingest (`pipeline.py`, env.payload RAW → caps+RFC1918 в db) + одноразовый идемпотентный `_backfill_printer_ip_map` в `init_db`; ридеры `get_printer_ip(device,printer)` (indexed) + `iter_printer_port_map` (offline); `_is_rfc1918`/`_clean_port_hint` server-side ревалидация; в `_DEVICE_TABLES` (purge/delete чистит). Gate green cov 92.74%, smoke OK; Opus security-review=APPROVE-WITH-NITS 0 crit/high (3 нита применены: non-list guard + 2 коммента). Тесты `tests/test_printer_ip_map.py` (12).** **P2+P3 СМЁРЖЕНЫ+ЗАПУШЕНЫ в origin/main (`a4054f5`, 2026-06-23): P2 = immutable `PrintFilter`{date_from,date_to,device,printer,ip} + `_print_where` (ПОЛНОСТЬЮ параметризован; даты→локальный день→UTC-cutoff, date_to включительно, reversed-swap) + `_PRINT_BASE_FROM` (print_jobs⨝devices⨝printer_ip_map; PK(device_id,printer)→<=1 строка, JOIN не множит); P3 = `db.get_print_summary(PrintFilter)` + `GET /fleet/print/summary` (7 метрик: total_pages·total_jobs[ТОЛЬКО events=COUNT(job_id)]·active_printers·active_computers·busiest_printer{+repr.ip=MAX(m.ip)}·most_active_computer·avg_pages_per_job; counter-страницы входят в total_pages, НЕ в jobs) + api `_q`(strip+256cap)/`_print_filter`. Gate green cov 92.83%, bandit/smoke OK; Opus security-review=APPROVE-WITH-NITS 0 crit/high (SQL-inj инвариант держится by construction: в f-string только `_PRINT_BASE_FROM`-литерал + where-с-плейсхолдерами, ВСЕ значения биндятся; +тест date-injection). Тесты `test_print_query_layer.py`(8)+`test_print_summary.py`(9).** **P4+P5+P6 (+P11-backend +P12-индексы) СМЁРЖЕНЫ+ЗАПУШЕНЫ в origin/main (`59de25c`, 2026-06-23):** P4 = `db.get_print_series(PrintFilter, granularity=auto, max_series=12)` + `GET /fleet/print/series` (GROUP BY (device_id,printer,strftime-bucket); авто-детализация час≤2д/день≤45д/неделя≤180д/иначе месяц через whitelist `_GRAN_FMT`; нет дат→span из MIN/MAX(ts); топ-N пар + серия «прочее»; `_assemble_series` выравнивает точки по общей оси buckets; label=hostname→printer, ip отдельным полем). P5 = `db.get_print_records(f,page,page_size,sort,dir,q)` + `GET /fleet/print/records` (серверная пагинация page_size≤200; whitelist `_RECORDS_SORT` + dir asc/desc; поиск q LIKE по hostname/printer/ip с ЭКРАНИРОВАНИЕМ `%`/`_`/`\` + ESCAPE; api `_label_print_row` добавляет RU `source_label`/`validation`/`validation_color` events=точно/good·counter=оценка/warn; ip=null где не резолвится). P6 = `db.get_print_filter_options(date_from,date_to)` + `GET /fleet/print/filter-options` (DISTINCT в периоде); `export_print_rows(PrintFilter)` (было days) + колонки `ip`,`validation`; api `_csv_safe` (защита от формульной инъекции `= + - @ \t \r`) + `_export_filename`/`_filename_date` (СТРОГАЯ ISO-валидация — нет header-injection в Content-Disposition). P11-backend = `get_print_analytics(days,date_from,date_to)` (явный диапазон переопределяет days, prev-период тогда пропускается; legacy days без изменений) + endpoint. P12 = индексы `idx_print_ts`,`idx_print_printer`. Весь SQL параметризован (в f-string только литералы/`_PRINT_BASE_FROM`/whitelist-фрагменты + `_print_where`-плейсхолдеры). Gate green cov 92.95%, ruff/mypy/bandit/smoke OK; Opus security-review=APPROVE-WITH-NITS 0 crit (1 HIGH header-injection + 1 MEDIUM LIKE-escape + 1 LOW тест — ВСЕ исправлены). Тесты: test_print_series(8)+test_print_records(10)+test_print_filter_options(11)+test_print_analytics_range(2). **P7–P11 (UI) СМЁРЖЕНЫ+ЗАПУШЕНЫ в origin/main (`e3d8758`, 2026-06-23):** `print.html` полностью переработан (JS-driven, потребляет эндпоинты P4-P6) + route `print_analytics` отдаёт контекст фильтров. P7 = sticky-панель (диапазон дат + пресеты 7/14/30/90/Всё, селекты компьютер/принтер/IP за период, URL-state, `currentFilters()`, селекты URL-seeded после filter-options — сервер пре-заполняет только date-инпуты). P8 = hero Plotly «компьютер→принтер» (auto-detail badge, легенда ПК→принтер[IP] через `legendName`+srpEsc, «прочее»-fold, переключатель столбцы/линии, stacked-bar). P9 = 7 карточек (`textContent`, без XSS). P10 = детальная таблица (server paging/sort/search; whitelist arrows; counter=«оценка» chip; IP «—»). P11 = CSV-кнопка под фильтры + `@media print` + старые секции (timeline/printers/users/departments) на общий диапазон через `/analytics`. **ФИКС гонки Plotly:** init гейтится на `DOMContentLoaded` (defer-скрипт Plotly успевает выполниться; иначе быстрый localhost-fetch резолвится до загрузки Plotly → newPlot бросает в .then как UNHANDLED REJECTION, график молча не рисуется). Живой headless-browser E2E (Playwright): карточки/таблица/селекты/hero рисуются, XSS-payload `<img onerror>` инертен (0 img, ячейка escaped, window-флаг не выставлен). Gate green cov 92.95%, smoke OK; Opus security-review=APPROVE-WITH-NITS 0 crit/high (все sink'и через srpEsc; 1 нит r.jobs→num() применён). Тесты test_print_page(4) + обновлён guard test_print_chart_sinks (унифицированные sink'и). **P12 ЗАВЕРШЁН:** индексы `idx_print_ts`/`idx_print_printer` (в P1-данные-ветке); perf на 120k строк — records(printer)→`idx_print_printer`, series(range)→`idx_print_device_ts` (НЕТ full-scan), default/filtered 11-51мс, all-time(120k) ~382мс (ок для не-дефолтного «Всё»); security-review ×2 (SQL P4-6 + XSS P7-11) APPROVE; память `[[print-tab-rework]]` (+2 гочи: Plotly-DOMContentLoaded-гонка, CSV-имя-файла header-injection). **ВЕСЬ ТРЕД PRINTVIEW (12 фаз) ЗАВЕРШЁН И В origin/main.**
- **АКТИВНЫЙ ТРЕД (2026-06-20): NETDISCO** — подсистема Network Discovery (авто-обнаружение сети + персистентный L2/L3 граф топологии), 13 фаз, по RFC `docs/superpowers/specs/2026-06-20-network-discovery-rfc.md` + план `docs/superpowers/plans/2026-06-20-network-discovery-plan.md`. Server-side `server/netdisco/` обобщает принтерный движок. OFF-by-default, contract additive-only, агент не трогаем. Память: `[[project-netdisco]]`. **СТАТУС: Фазы 1–5 СМЁРЖЕНЫ И ЗАПУШЕНЫ в `origin/main` (P1–4 @ `4d6cf01`, P5 @ `c2b5c82`/ledger `ae44afb`, 2026-06-20, по команде владельца; gate green cov 92.77%, security-review Opus=APPROVE 0 crit/high).** **P6 (SNMP probe + classify) СМЁРЖЕН И ЗАПУШЕН в origin/main (`cfebc15`, далее push `f735e19`; gate green cov 92.68%, security-review Opus=APPROVE 0 crit/high/med).** **NETDISCO ВКЛЮЧЁН в shipped `server/config.json` (`6e341c2`): `netdisco_enabled`+`netdisco.enabled`/`active_scan`=true — по правилу владельца «ничего не выключено по умолчанию» (inventory+classify+active-scan loops работают; active LAN scan может триггерить EDR/IDS — принято).** **ДВА НОВЫХ ПРАВИЛА ВЛАДЕЛЬЦА (2026-06-21):** (1) `[[no-disabled-by-default]]` — OFF-в-коде фича обязана быть ON в `server/config.json` или тогглиться в дашборде, иначе включаю сам; (2) `[[auto-merge-push-no-ask]]` — НИКОГДА не спрашивать про merge/push, авто merge+push origin после каждого gate-green изменения (заменяет §6 «push only when asked»). **P7 (passive SNMP ARP/route harvest) СМЁРЖЕН И ЗАПУШЕН в origin/main (`bbc6970`; gate green cov 92.64%, security-review Opus=APPROVE 0 crit/high/med — triple RFC1918-gate, read-only, fail-closed parse traced, bounded 4096-walks).** **P8 (LLDP/CDP/FDB evidence + L2-inference §4.3) СМЁРЖЕН И ЗАПУШЕН в origin/main (`ec509dc`, 2026-06-22; gate green cov 92.82%, Opus security-review=APPROVE 0 crit/high, 2 LOW применены).** **P9 (Data Fusion §4.4 + reconcile + persist links) СМЁРЖЕН И ЗАПУШЕН в origin/main (`15e8b98`, 2026-06-22; gate green cov 92.83%, Opus security-review=APPROVE 0 crit/high/med, 1 LOW применён).** **P10 (Graph §3.5 + reachability-корреляция §1.5/§3.7 + change-detection/ghost-lifecycle §3.13) СМЁРЖЕН И ЗАПУШЕН в origin/main (`af0001f`, 2026-06-22; gate green cov 92.75%, Opus security-review=APPROVE 0 crit/high/med/low).** **P11 (read-only topology API + TTL-кэш графа §3.15 + телеметрия §3.16) СМЁРЖЕН И ЗАПУШЕН в origin/main (`4f01d08`, 2026-06-22; gate green cov 92.81%, Opus security-review=APPROVE 0 crit/high).** **P12 (Web UI «Топология») СМЁРЖЕН И ЗАПУШЕН в origin/main (`5261e28`; коммит `58a477c`, branch удалён; 2026-06-22; gate green cov 86.04% [полный сьют], smoke OK; Opus security-review=APPROVE 0 crit/high, 2 LOW: нет DB CHECK на формат device_nid [слугификация только в коде identity.device_nid] + добавлена позитивная XSS-проверка `&lt;/script&gt;` в тест [применено]).** Новое: `web/templates/topology.html` — SSR-таблица инвентаря (доступна без JS) + canvas force-граф настоящих L2/L3-линков (глифы по dev_type router=ромб/switch=квадрат/ap=треугольник/agent=круг/прочее=точка, цвет по статусу достижимости, рёбра по confidence-бэнду + пунктир ambiguous, клик→`/netdisco/device/{nid}`, JSON-остров `{{ graph|tojson }}`, 3 темы + reduced-motion); `web/templates/net_device.html` — карточка (интерфейсы/связи/статус/журнал изменений); routes `/topology`+`/netdisco/device/{nid}` в `dashboard.py` (+RU-хелперы net_type_ru/net_status_ru/net_conf_color/net_change_ru — machine-значения остаются English, тесты пинят); nav «топология» в `base.html`. XSS-защита: `|tojson` Unicode-escape `<>&` (остров не рвётся) + autoescape ON + тултип через `textContent` (не innerHTML) + href фиксированным префиксом + `encodeURIComponent` (нет javascript:). `tests/test_topology_web.py` (6). Следующая = **P13 (Credentials hardening: непубличная SNMP community через DPAPI-шифр-хранилище `netdisco/credentials.py` — ctypes CryptProtectData/CryptUnprotectData machine-scope, stdlib-only; community НЕ в логи/API/дашборд; не-Windows fallback=public; `CredentialRef`-модель с заделом под SNMPv3; security-review ОБЯЗАТЕЛЕН; до P13 только community=public)**. P11 carry-forward (НЕ блок): (a) network_risk confidence-модулятор при unreachable (blind-spot MEDIUM-cap) ОТЛОЖЕН — не трогал scoring-ядро в этой сессии (опц., §3.7 read-side); (b) device-detail не отдаёт reading-серию (нет writer'а net_device_readings ещё); (c) LOW из review: ленивый GraphCache-init в хэндлере (app.state) — косметика, можно поднять в startup (без security-эффекта, single-slot+lock). P10 carry-forward (НЕ блок, из security-review): ARP-адреса попадают в `net_devices.ip` без ingest-time RFC1918-гейта — покрыто probe-time double-gate (reconcile+scan оба перепроверяют `is_rfc1918`); если будущий код станет пробить stored ip БЕЗ перепроверки — дыра оживёт (держать инвариант). P10 carry-forward-2: reachability пингует ВСЕ RFC1918-устройства inventory (вкл. endpoints) каждый цикл (нужно для каскада «за-шлюзом→unreachable»); на большом LAN — нагрузка, опц. cap/батч. P9 carry-forward (НЕ блок): fusion отбрасывает `local_if` улики (ResolvedLink без a_if/b_if — порт не пишется в net_links; опц. вернуть для карты); cross-source MAC↔chassis node-merge отложено (один узел под nd-mac и nd-chassis не сливается без инвентаря — §4.6, заберёт graph/inventory). P8 carry-forward (НЕ блок): walk'и evidence на дефолтном `max_rows=512` (большой FDB усечётся — для часового topology-цикла ок; опц. поднять для свитчей). P7 carry-forward (НЕ блок): route `cidr`/`ifindex` отбрасываются на call-site (только next_hop → кандидаты) — заберёт topology-фаза. P6 carry-forward (НЕ блок): LOW-1 `scheduler._fleet_agent_macs` зовёт приватный `netmap._agent_macs` (дублируется в `inventory.py`) → вынести публичный `agent_macs(snapshots)` (durability self-probe-гарда); LOW-2 ifTable = 5×`walk(max_rows=512)` на отзывчивый хост (ок для часового цикла; опц. cap 256). P5 carry-forward (НЕ блок): M1 пустой `scan_cidrs`→авто-/24; M3 SNMP community cleartext `public` (v1/v2c → P13 DPAPI). Done-фазы (P6→P1) ниже.
- **ПОБОЧНЫЙ ФИКС (2026-06-23): /printers «Напечатано страниц» был пуст** — график строился из snapshot `total_pages` (пусто без счётчика). Заменён на ИСТОРИЮ (спец §9 «история счётчиков, тренд»): новая `db.get_printers_pages_series(days, max_printers=12)` (топ по latest total_pages, per-printer временной ряд из `printer_readings`, NULL-счётчики пропущены, days-окно f-string по clamp'нутому int — паттерн `get_printer_print_summary`), route `/printers` отдаёт `pages_series`, `printers.html` — мультилинейный Plotly scatter (был horizontal bar), data-island `pages-series-data`, srpEsc на label, пустой-стейт «нужно ≥2 опроса». Gate green cov 92.78%, mypy/bandit/ruff/smoke OK. Branch `fix/printers-history-chart`. Память: `[[printer-dashboard-reconcile]]`.
- Evolve SRP (Windows PC failure early-warning) from MVP toward production per `cctodo.md`.
- Immediate thread: **CLOSE P0 data-integrity foundation (W0.1/W0.2/W0.5/W0.4)** before any analytics (§4/W4.*). Strategy §8: nothing in Analytics is trusted until P0 closed.
- Success: append-only longitudinal history (trends + labels possible); server-stamped time; scores degrade to UNKNOWN/insufficient-data under low coverage (D2 enforced at score level); CONTRACT_VERSION compat tested.

## RE-PLAN (2026-06-01) — strategy-driven, follow to end
- **Finding:** ledger jumped to P1 deployability (W1.x) after W0.3, but P0 §2 items W0.1/W0.2/W0.4/W0.5 remain OPEN (verified in code: `db.py` historical+scores still PK(device_id) overwrite; `pipeline.py` trusts `env.ts`; `scores.py` starts at 100 with no coverage gate). Strategy §8 mandates ALL P0 before Analytics.
- **Decision:** close P0 in dependency order; remaining P1 polish (signed config, transport jitter/idempotency, rate-limit) deferred behind P0 (§3 = "without ceremony", non-blocking).
- **Order this run:** (1) ✅ W0.1 [merge `a6f3de2`] → (2) ✅ **W0.2 received_at+clock-drift [MERGED `403f3f7`]** → (3) ✅ **W0.4 [MERGED `b58a166`]** → (4) ✅ **W0.5 Score100 [MERGED `341bbdb`]**. **✅ ALL P0 CLOSED + REVIEWED — `main`↔`origin` in sync.** W0.5 **post-merge security-reviewed** (`bd8a61d`): both HIGH = false-positive (verified — observability penalises-not-withholds untrusted *by design* (emits bad, never false-healthy); guarded `trust is None` makes the claimed AttributeError unreachable); minor hardening applied (untrusted-observability → low confidence not high, zero-domain → UNKNOWN, named risk threshold, +2 tests). W4 analytics unblocked. Each: branch → TDD → gate green → subagent review → `merge --no-ff` → `push origin main` → ledger update.

## Constraints/Assumptions
- System class: **high-trust degradation detection platform**, NOT "AI predicts failures". Prime directive: under uncertainty → UNKNOWN, never guess.
- Scope = **Phased/gated**: ML/survival/labels deferred (not killed). Deployable-but-not-deployment-driven; telemetry-trust = absolute P0.
- **Scope ceiling**: no nested confidence / evidence DAGs / confidence calculus. Stop at state/weight/collector_trust/semantic_trust/freshness.
- Thin agent invariant: `client/` pure stdlib, zero deps. Language-independent collection (CIM classes, numeric event Level). Serial hashed in agent.
- Python 3.9 floor, explicit `Optional`, line-length 100, double quotes. Immutable patterns, files <800 / funcs <50 lines.
- `make check` green before every code commit (lint/mypy/bandit/coverage≥80%). CHANGELOG line for visible-behavior changes. No commit attribution.
- Git: branch-first → `merge --no-ff` → `push origin main`. `.codegraph/` gitignored. Auto-format hook (`ruff --fix`+`format`) runs on every `.py` edit (strips not-yet-used imports; collapses comment alignment — accept it).
- User authorized continuous execution + all run/subagent permissions; maintain this ledger every turn; use subagents + skill where viable (note: large subagent dispatches repeatedly hit session limits → implementation often done directly, reviews via subagent).

## Key decisions
- `state` = authoritative gate; `weight` = modulation only (never reanimates a gate-failed source).
- `collector-trust ⊥ semantic-trust`: agent reports collection facts; server judges plausibility (A1).
- **Materiality governor**: semantic validation only for decision-material signals; thermal = frozen-check only.
- **UNKNOWN** first-class outcome; untrusted identity → device scores withheld (contract §7).
- F3 v1 validators: stateless (range/cross-field/known-bad) + frozen-on-last-good; trend-based deferred.
- **3c mapping (decided):** bayesian CLASS→trust DOMAIN gate — storage→storage, battery→battery, power_thermal→thermal, stability→os_stability; **memory ungated**; disk_fill/boot tracked but gate no class (v1).
- **3e:** reuse `source_last_good` — a source with a stored last-good that now reports a collector failure = **regressed (newly-blocked)**; flag in lineage + dashboard. Distinguishes "was capable, now lost it" from "never seen".
- **W1 design (decided, governor-driven):** `site_code` = grouping/identity only (no auth/isolation now); certs folded into `historical` (no new msg_type), never read private keys, surfaced directly (not a bayesian class); dashboard = vanilla-JS poll (no SPA/websocket); feedback = ack+note only (no remote agent control). Each piece justified; nothing speculative.
- **Identity-labels (NOT tenancy) — designed+DESCOPED 2026-06-02, DEFERRED behind P0; integrated into `cctodo.md` §3:** org/dept = additive numeric LABELS extending `site_code` (server-side name directory `organizations`/`departments`; client never gets names; contract additive, no version bump; edge `0`/'' → «no dept»/NULL). Onboarding = interactive numbers + `install-service.ps1` args (GPO/SCCM). User confirmed **external-only threat model** → existing global `ingest_token` (merged `b49a738`) is the sufficient anti-spoof boundary. **Data-isolation / per-org secrets / dashboard-auth NOT built** — `cctodo.md` §1 anti-goal «multi-tenant orchestration» + D7; parked until a real 2nd mutually-distrusting customer (D8 keeps the option via additive labels). Storage-isolation Q moot (nothing to isolate — I over-asked). Why → memory `[[identity-labels-not-tenancy]]`.

## State

### Done
- **NETMAP-UNIFICATION Ф5 — интерактивная панель управления «гениальной карты» [СМЁРЖЕН+ЗАПУШЕН origin/main, 2026-06-25; gate green cov 92.82% (1322 passed), ruff/mypy(107)/bandit clean, smoke OK; Sonnet code-review REQUEST-CHANGES→исправлено (CRITICAL+HIGH+MEDIUM+6 LOW), Opus security APPROVE-WITH-NITS (0 crit/high; XSS-invariants PASS)]:** карта превращена в операторский центр сети. ВСЁ CORE клиентское поверх единого графа Ф2 (ноль новых серверных моделей); единственное серверное дополнение — read-only «машина времени». **Сервер:** `db.list_topology_snapshots(limit)` (newest first, clamp 1..500, параметризован) + `db.get_topology_snapshot(snapshot_id)` (bound int, fail-closed None на non-positive/garbage/missing) рядом с `get_latest_topology_snapshot`; `GET /api/v1/network-map/snapshots` (список для слайдера, counts-only); `GET /api/v1/network-map/graph?at=<id>` + `/netmap?at=<id>` (SSR) — исторический кадр через **единый helper `netdisco/unified.py::historical_graph_from_snapshot(snap)`** (ОДИН источник формы → API и SSR не расходятся → плашка `history_at` рендерится на обоих); **КРИТИЧНО: `?at` МИНАЕТ live `GraphCache`** (исторический кадр НИКОГДА не отравляет кэш; `test_api_graph_at_bypasses_live_cache` пинит); live-оверлеи (качество/аномалия/identity) НЕ считаются (D5); плохой id → 404. **Клиент (`_netgraph.html` rewrite, состояние=`state`):** фильтры (тип/статус/medium/confidence/provenance/подсеть-select + текстовый поиск) + пресеты (только инфра / скрыть конечные / **скрыть неподтверждённые** = переинтерпретация «скрыть ARP-only» для unified-графа, т.к. ARP-only узлы уже исключены в Ф2 / сброс; предикаты `nodeOk(n)`+`linkOk(L)`); тоггл слоёв (L2/L3/uplink/wireless/quality/groups/ports); раскладки free/subnet/type + персист позиций `localStorage["srp-netmap-positions"]` (по nid); **боковая панель `ng-side` на клик** (`showSide` через `kv()`=`textContent`, навигация по кнопке `card_url` — контекст карты держится, НЕ слепой клик); tooltip ребра (`showEdgeTip`: via_source/confidence/medium/порты/качество) + подсветка соседей; «сохранить вид» `localStorage["srp-netmap-view"]` (`restoreView` **merge'ит** поверх дефолтов, не wholesale replace); экспорт PNG (`canvas.toBlob`)/CSV (nodes+links + **формульная защита** `csvSafe`: `= + - @ \t \r`)/JSON. **ADVANCED (клиентская BFS над `adj={[nid]:[соседи]}`):** изоляция N-hop (`isolateSet`), путь (`pathBfs` — computed-key sentinel `prev[a]=null`, НЕ объект-литерал; regression-пин `test_netmap_path_engine_terminates` ловит баг-форму, которая зацикливала вкладку), первопричина (`causeResult`: down vs UNREACHABLE); **ADV-результаты кэшируются в `navCache`** (пересчёт только в `recomputeNav()` на смене state, НЕ каждый кадр). «Машина времени»: слайдер по `window.__NETMAP_SNAPSHOTS` (JSON-остров `netmap.html` `|tojson`) → `?at=<id>`; плашка + кнопка «вернуться к живой карте» (рендер по `IS_HISTORY=!!G.history_at`); save-view/export-json disabled в историческом кадре. Горячие клавиши F/R/Esc. **XSS (Opus PASS):** 0 `innerHTML`/`insertAdjacentHTML`/`outerHTML`; все agent/SNMP-строки → `textContent`/`fillText`(canvas — не HTML-sink); граф+snapshots → `|tojson` (autoescape ON); click-through = только `card_url` (серверный, нельзя собрать `javascript:`); `window.location.href` — только сервер-относительные + `encodeURIComponent`; CSV-защита. **Применено из ревью:** CRITICAL `pathBfs` (computed-key) + regression-пин; HIGH shared-helper `historical_graph_from_snapshot` (plaque divergence) + regression-пин `test_netmap_time_machine_plaque_on_ssr_route`; MEDIUM `navCache` (recompute-on-state-change); LOW — dead-code (linkMedium/dead-var/syncPanelUI-stub), `fit()` на toggle фильтров, `restoreView` merge, реальный disable в историческом кадре. Тесты `test_netmap_web.py`(+8: панель/пресеты/машина-времени-mount/исторический-SSR/no-innerHTML/XSS-pin×2 + 2 regression-пина pathBfs/plaque) + `test_netmap_history.py`(20: db readers + API `?at`/`/snapshots`/cache-bypass/404). CHANGELOG RU (Added). Память `[[netmap-control-panel]]` + `[[netmap-unification]]` (Ф5 добавлен, NEXT=Ф6). Контракт агента НЕ тронут (нет bump). **NEXT = Ф6 (каноническая карточка) — R3+review.**
- **NETMAP-UNIFICATION Ф4 — единый canvas-движок карты сети [СМЁРЖЕН+ЗАПУШЕН origin/main merge `d035fac` (no-ff, commit `5bcb2c6`, branch удалён), 2026-06-25; gate green cov 92.80% (1306 passed), ruff/mypy(107)/bandit clean, smoke OK; Sonnet code-review=APPROVE-WITH-FIXES (0 crit/high; XSS-invariants PASS; 2 LOW применены: stale-Ф10-коммент + расширенный XSS-pin `<img onerror>`):** один canvas `web/templates/_netgraph.html` (на базе `topology.html`) рисует ОБЪЕДИНЁННЫЙ граф Ф2 `{nodes,links,subnets,totals}` через Ф3 `GraphCache`. Визуалы обеих страниц в одном движке: физика/зум/пан/пин/поиск/3-темы/`textContent`-tooltip + кольца подсетей, цвет-качество на agent-uplink, пульс «пакета» (гибнет на полпути при потерях), кольца-аномалии на шлюзах деградировавших подсетей, пунктир `medium=wireless`+`ambiguous`. Глифы ВСЕХ типов (router=ромб / switch+printer=квадрат / ap+phone=треугольник / agent=круг-цвета-здоровья / server=шестиугольник / endpoint+unknown=точка); цвет по статусу достижимости, для агента — по потерям до шлюза (`quality_overlay` на agent-uplink линке); подписи для крупных узлов. Клик → каноническая карточка `card_url` (agent>printer>net, из ассемблера, НЕ сырой href). `netmap.html` стал обёрткой: toolbar (search id `netgraph-search` + кнопка «⟳ Собрать карту сейчас» → `POST /api/v1/topology/poll` + статистика `graph.totals`) + `{% include "_netgraph.html" %}` + SSR-таблица инвентаря (по `card_url`, без JS) + журнал изменений 7д. Маршрут `/netmap` (`dashboard.network_map`) теперь отдаёт единый граф через `_unified_map_graph` (= тот же `app.state.network_map_cache`, fallback `_EMPTY_GRAPH` с totals-ключами как у ассемблера) + `db.get_net_changes(7)`; старая эфемерная кластерная модель `build_netmap` ушла СО СТРАНИЦЫ (осталась в `analytics/netmap.py`, используется `_attach_printers_to_netmap` + `test_netmap.py`/`test_printers_page.py` — НЕ трогать). Симметричный glyph-апгрейд: связанный принтер (`printer_id`+`dev_type=unknown`) → `printer` (как агент; `device_id` приоритетнее, реальный тип NEVER не перетирается). Безопасность: все DOM-sinks = `textContent` (0 `innerHTML`/`insertAdjacentHTML`/`outerHTML`), граф встроен через `|tojson` (autoescape ON → `</script>` breakout инертен), click-through = только `card_url` (серверный, из device_id/printer_id/nid), ошибка poll через `srpEsc`. Применены из ревью: L-1 doc-коммент `_netgraph.html` (убрана преждевременная ссылка на Ф10), L-2 +тест `test_netmap_page_inert_against_event_handler_payload` (`<img onerror>`/`<svg onload>`/`javascript:` НЕ появляются как live-теги, только экранированные `&lt;img`). Тесты `tests/test_netmap_web.py`(13: переписаны под unified-форму — единый граф/island/canvas-id/wireless-medium/все-типы/ARP-only-скрыт/anomaly/XSS-pin×2/SSR-инвентарь/кнопка+журнал/empty-state) + `tests/test_netmap_unified.py` регресс (printer-glyph-upgrade). CHANGELOG RU (Changed). Память `[[netmap-unification]]` (Ф4 добавлен). **NEXT = Ф5 (панель управления: фильтры/слои/раскладки/боковая панель/сохранить-вид/экспорт; ADVANCED изоляция/путь/первопричина/машина-времени) — R3+review+XSS.**
- **NETMAP-UNIFICATION Ф3 — единый API карты сети [СМЁРЖЕН+ЗАПУШЕН origin/main merge `9c6ae9f` (no-ff, commit `0d80164`, branch удалён), 2026-06-25; gate green cov 92.56% (1301 passed), ruff/mypy/bandit clean, smoke OK; Sonnet code-review=APPROVE-WITH-FIXES (0 crit/high, все LOW/MEDIUM применены):** один канонический граф-контракт. Новый `GET /api/v1/network-map/graph` → единый граф `{nodes,links,subnets,totals}` (Ф2-ассемблер) через `GraphCache`. Новый loader `server/netdisco/cache.py::load_network_map()` (теперь ДЕФОЛТНЫЙ loader кэша вместо `db.get_latest_topology_snapshot`): DB fan-out (`get_net_devices`/`get_net_links`/`get_network_snapshots`/`get_printers`)→`build_network_map`; loader НИКОГДА не None (пустой флот→пустой well-formed граф) → `get()` отдаёт сам граф (НЕ обёрнутый в `{graph, received_at}`). Deprecated-АЛИАСЫ `GET /api/v1/netmap` и `GET /api/v1/topology/graph` → тот же `_network_map_graph` (один кэш-объект → СТРУКТУРНО идентичный граф, не совпадение); ФОРМА ОТЛИЧАЕТСЯ от старой: `nodes/links/subnets/totals`, НЕТ `graph`-обёртки, НЕТ `received_at` (отметка осталась на SSR-странице `/topology` через snapshot). `/discovery/poll`+`/topology/poll` инвалидируют кэш ПОСЛЕ работы (`_invalidate_network_map_cache`); фоновые циклы НЕ инвалидируют (TTL 45с поглощает steady-state). `create_app` (main.py) создаёт `app.state.network_map_cache=GraphCache()` up-front → P11-LOW ленивый init в хэндлере снят (fallback `getattr(...) or GraphCache()` только для app вне `create_app`). `GraphCache.get()` держит lock на весь rebuild → холодный/expired rebuild блокирует concurrent readers (один build на TTL-окно by design; serve-stale = future opt-in, задокументировано в docstring). Веб `/netmap`(`build_netmap`)+`/topology`(`get_latest_topology_snapshot`) НЕ тронуты → перешли на единый граф в Ф4. Безопасность: API=`application/json` (`</script>` в hostname инертен, браузер не парсит как HTML), кэш не отравляется request-вводом (граф целиком из БД, нет per-request ключа), нет секретов/community/публичных-IP (RFC1918-only на сборе). Применены из ревью: L-1 убран мёртвый lazy-init-код, L-2 CHANGELOG note про пропавший `received_at`, M-1 perf-коммент про lock-сериализацию rebuild, L-3/L-4 тесты (reload-после-TTL + legacy-форма-ушла). Тесты `tests/test_netmap_unified_api.py`(8: форма/empty/кэш-no-rebuild-в-TTL/reload-после-TTL/алиасы=тот-граф/invalidate-after-оба-poll/XSS-inert)+обновлённые `test_netdisco_api.py`/`test_netmap_web.py`. CHANGELOG RU (Changed+Deprecated). Память `[[netmap-unified-api]]`.
- **NETDISCO UI — кнопка force-build топологии + скрытие ARP-only узлов на «Карте сети» [СМЁРЖЕН+ЗАПУШЕН origin/main merge `f212e0b` (no-ff, commit `f98354e`), 2026-06-22; gate green cov 92.79%, smoke OK; Opus security-review=APPROVE 0 findings]:** (1) Страница «Топология» получила кнопку «⟳ Собрать топологию сейчас» → `POST /api/v1/topology/poll` (api.py) форсит один `reconcile.run_topology_cycle(cfg)` сейчас (read-only SNMP известной RFC1918-инфры → fuse → rebuild+persist граф), инвалидирует `GraphCache`, страница перезагружается; дальше фоновый `_netdisco_topology_loop` обновляет сам. Rate-limited (`endpoint:topology_poll`, ПЕРЕД работой) + shared `_poll_lock` (concurrent→busy), зеркало `/discovery/poll`. cfg из `app.state.netdisco_config`, fallback `load_netdisco_config(None)` (fail-closed disabled). `topology.html`: кнопка + `pollTopology()` (результат в `textContent`, ошибка через `srpEsc`). (2) «Карта сети»: `netmap._attach_neighbors` БОЛЬШЕ не собирает безагентные ARP-only `others` (шлюз MAC/vendor по-прежнему извлекается); `others` теперь всегда `[]` → форма `/api/v1/netmap` НЕ изменилась. `netmap.html`: убраны стат «без агента», ключ легенды, SSR-чипы и canvas-создание other-узлов. На карте остаются только агенты, шлюзы и принтеры (полный L2/L3-граф — на «Топологии»). Тесты: topology poll (runs/busy/rate-limit/cache-invalidate +4) + netmap ARP-hidden (unit rewrite 2 + web +1). Security-review (Opus)=APPROVE: RFC1918+read-only egress, rate-limit-before-work, anti-DoS lock, нет утечки community, fail-closed config, XSS-safe (textContent/srpEsc/|tojson). **Пост-P13 UI-доводка (netdisco 13/13 уже готов).**
- **PRINTERS fix — фантомные ARP-принтеры + пустой график страниц [СМЁРЖЕН+ЗАПУШЕН origin/main merge `bdd685e` (no-ff, commit `771bc2a`, branch удалён), 2026-06-22; gate green cov 92.83%, smoke OK; Opus security-review=APPROVE 0 crit/high]:** Корень: `discovery.merge` берёт КАЖДОГО ARP-соседа кандидатом, а `run_poll_cycle` хранил ВСЕХ → любой хост LAN (ПК/телефон/роутер) оседал «принтером» status=unreachable, model/serial/total_pages=NULL → засорял `/printers` и оставлял график «страниц (HW)» пустым (у не-принтеров нет счётчика). Fix: `scheduler.run_poll_cycle` НЕ хранит кандидата если `reading is None` ∧ `_arp_only(sources)` ∧ ¬`is_confirmed(pid)` (новый `skipped`-счётчик; подтверждённый принтер в офлайне всё равно пишет unreachable — «down≠gone»; spooler/config/scan хранятся); `poll_now` зовёт `purge_phantoms=db.delete_unconfirmed_arp_printers` после цикла (чистит легаси). `db.py`: `printer_is_confirmed(pid)` (model∨serial∨total_pages), `delete_unconfirmed_arp_printers()` (один `_lock`-скоуп: читает victims подзапросом latest-detail + удаляет printers+printer_readings — нет TOCTOU, фикс из ревью), `_printer_is_confirmed_row`/`_printer_is_unlisted_arp`; `get_printers_overview` фильтрует фантомы. График (`printers.html`) уже корректен — фильтрует `total_pages!=null`; пайплайн total_pages←prtMarkerLifeCount в `drivers/standard.py:178` исправен → заполнится по реальным принтерам. Тесты: +6 scheduler (skip/arp-real/confirmed-down/spooler), +3 db (confirmed/purge/overview-hide), обновлены 4 на новый dict-контракт (`skipped`). CHANGELOG RU (Fixed). Локальный srp.db: 3 фантома вычищены. cctodo: добавлен блок «Сделано вне дорожной карты» (принтеры + netdisco 13/13). Security-review (Opus)=**APPROVE**: SQL параметризован, delete-предикат не трогает реальные/офлайн-принтеры, TOCTOU закрыт; LOW: unbounded delete (ок под `_poll_lock`), `counts[tag]` без guard (теги исчерпывающи).
- **NETDISCO Phase 13 — DPAPI credential store for non-public SNMP community [SAFETY; СМЁРЖЕН+ЗАПУШЕН origin/main merge `aacaabb` (no-ff, commit `9367bae`, branch удалён), 2026-06-22; gate green cov 92.82%, smoke OK; Opus security-review=APPROVE]:** Новое `server/netdisco/credentials.py`: `CredentialStore` (JSON-файл base64-обёрнутых DPAPI-blob'ов; на диске ТОЛЬКО шифртекст) + ctypes-обёртки `protect`/`unprotect` над `CryptProtectData`/`CryptUnprotectData` (machine-scope `0x4`, stdlib-only, owned-buffer `from_buffer_copy`, `LocalFree` в finally) + `dpapi_available()` + `CredentialRef` (frozen, SNMPv3-задел user/auth_key/priv_key, `__repr__`/`__str__` НЕ показывают ключи) + `resolve_community(cfg, store)` (НИКОГДА не raise; нет ref→`cfg.snmp_community` [warn если non-public plaintext, значение НЕ логируется]; есть ref→store.get или fail-safe `public`) + `default_store()` (None вне Windows). `config.py`: `+snmp_credential_ref: str=""` (parse, non-str→""). Call-sites `scheduler._make_session`/`scan.run`/`reconcile.run_reachability_cycle` теперь `resolve_community(cfg, store=default_store())` вместо `cfg.snmp_community`. `.gitignore` += `netdisco_secrets.json`. CHANGELOG RU (Security). 16 тестов `tests/test_netdisco_credentials.py` (round-trip фейк-DPAPI, on-disk-encrypted, no-leak, public-fallback, corrupt-b64→None, non-public-warn). Security-review (Opus)=**APPROVE**: 7 инвариантов §3.18 PASS (секрет только шифрован; community не течёт в лог/API/дашборд; non-Win/decrypt-fail→public; public-plaintext ок без store; stdlib-only; чужие приватные ключи не читаем; SQL/ingest не тронуты); `resolve_community` не raise — подтверждено; LocalFree корректен; hostile-input fail-closed. Применены: H-1 owned-buffer, H-2 атомарная запись (`tmp`+`os.replace`), H-3 ACL-зависимость задокументирована в `default_store` (installer `icacls /inheritance:r` на C:\SRP), M-1 `b64decode(validate=True)`, M-3 warn non-public-plaintext, M-4 `CredentialRef.__repr__` без ключей, L-3 corrupt-b64 тест, _MAX_STORE_BYTES 1MB cap. **NETDISCO 13/13 ФАЗ ГОТОВ.** Carry-forward (НЕ блок): community=public остаётся дефолтом shipped config (нечего «включать» — DPAPI-store opt-in: задать ref + провизионить секрет на Windows; [[no-disabled-by-default]] не применяется, секрет нельзя зашить в поставку).
- **NETDISCO Phase 11 — read-only topology API + TTL-кэш графа §3.15 + телеметрия §3.16 [merge `4f01d08` (no-ff; 2 коммита 37e4ba8→a339a55, branch `feat/netdisco-phase11-api-cache-telemetry`), 2026-06-22, ЗАПУШЕНО origin/main `443e486..4f01d08`]:** Новое: `netdisco/cache.py` `GraphCache(ttl_sec=45, loader=db.get_latest_topology_snapshot, clock=time.monotonic)` — read-through single-slot TTL-кэш (threading.Lock, `get`/`invalidate`, инъектируемые loader/clock); `netdisco/metrics.py` `ScannerMetrics`+`METRICS` singleton (thread-safe `observe_cycle(cycle, **counts)` → `{cycle}_cycles`+счётчики, `snapshot()` copy-on-read). `reconcile` циклы пишут METRICS (topology: probed/links/deltas; reachability: down/unreachable) — телеметрия живая. `api.py` 4 read-only роута: `GET /topology/graph` (per-app GraphCache на `request.app.state.netdisco_graph_cache`, lazy-init → пустой граф если нет snapshot), `/topology/changes?days` (clamp 1..365, `db.get_net_changes` параметризован, limit 2000), `/netdisco/devices/{device_nid}` (`get_net_device`→интерфейсы+линки+status reachability-аннотация, 404 если нет), `/netdisco/stats` (`METRICS.snapshot()`). 13 P11-тестов (cache 4, metrics 3, api +5, +device-detail/stats; netdisco всего 202). CHANGELOG RU (Added: topology API + телеметрия). Gate green: ruff/format/mypy[105]/bandit clean, cov **92.81%**, smoke OK. **Security-review (Opus)=APPROVE** (0 crit/high; SQL device_nid/days bound+параметризованы [нет инъекции], days clamp [1,365]+limit 2000 [нет unbounded scan], cache single-slot/TTL/Lock [не растёт, не отравляется request-вводом], METRICS-ключи внутренние [конечны]+copy-on-read, JSON-only [нет XSS-sink], нет утечки secret/community/keys/disk-serial — net_devices.serial = chassis-серийник железа server-side, не агентский диск). **1 LOW (carry-forward, НЕ блок):** ленивый GraphCache-init в хэндлере — косметика, поднять в startup (без security-эффекта). **Для P12 (Web UI топология, зависит P11, skill `frontend-design`):** `web/templates/topology.html` — SSR-таблица инвентаря (доступна без JS) + JSON-остров `{{ graph|tojson }}` + canvas-движок (ПЕРЕИСПОЛЬЗОВАТЬ `web/templates/netmap.html`: mulberry32 seeded-rng, пружинная раскладка, темы, reduced-motion); узлы-глифы по dev_type (router=ромб/switch=квадрат/ap=треугольник/agent=круг/endpoint=точка), рёбра по confidence/link_kind; **XSS: ВСЕ agent/SNMP-строки через `window.srpEsc` перед DOM-sink + `|tojson` для острова, autoescape ON `[[dashboard-xss-srpesc]]`**; `web/dashboard.py` route `/topology` (отдаёт graph+inventory+changes из P11-getters/API), nav-ссылка в `base.html`; карточка устройства (расширить `device.html` ИЛИ новый `net_device.html`): интерфейсы/линки/статус/история/изменения. XSS-пин-тест: agent-строка `</script>` не рвёт JSON-остров. security/code-review ОБЯЗАТЕЛЕН (MED, agent/SNMP-строки в HTML).
- **NETDISCO Phase 10 — Graph engine §3.5 + reachability-корреляция §1.5/§3.7 + change-detection/ghost-lifecycle §3.13 [merge `af0001f` (no-ff; 4 коммита e3637c4→9e1aa7c, branch `feat/netdisco-phase10-graph-correlation-changes`), 2026-06-22, ЗАПУШЕНО origin/main `27ce594..af0001f`]:** граф топологии оживает — корень сбоя + журнал изменений + lifecycle призраков. Новое (всё ЧИСТЫЕ функции): `netdisco/graph.py` `Graph(adjacency)` frozen + `build_graph(devices,links)` (undirected, изолированные узлы — ключи с пустым set, self-loop/None пропущены), `neighbors`, `reachable_from(roots,*,blocked)` BFS (blocked-узел не входит и не пройден → точные up-компоненты), `path_to_root(node,roots)` (кратчайшая цепь `[node..root]`, sorted-соседи=детерминизм, visited-set→циклы терминируют), `find_root_cause(down_set,roots)` (верхние down — сам root ∨ сосед up-reachable-компоненты); `netdisco/correlation.py` `correlate(graph,down_set,roots)→{nid:Verdict(status,root_cause,suppressed)}` (§3.7: down-узел чей путь к корню пересекает другой down → UNREACHABLE+suppressed, root_cause=upstream-down через `_upstream_cause` по path_to_root; иначе DOWN сам себе cause; КЛАССИФИЦИРУЕТ только членов down_set — НЕ выдумывает DOWN из blind-spot, D5); `netdisco/changes.py` `diff(prev,curr)→list[TopologyDelta(kind,device_nid,detail)]` (§3.13: appeared/disappeared/link_added/link_removed/reclassified/ip_changed; nid-keyed → DHCP-смена IP = ip_changed, НЕ phantom appeared+disappeared; canon-undirected линки; детерм. sort) + `stale_lifecycle(devices,*,now,stale_after_sec,purge_after_sec)→[(nid,status)]` (missing после устойчивого отсутствия, eligible_purge после долгого; единичный промах НЕ → missing; unparseable last_seen → не стареет; sorted). Wiring: `reconcile.run_topology_cycle` += diff(prev_snapshot,new_graph)→`store_net_change` журнал + `stale_lifecycle`→`db.set_net_device_status` (НОВ status-only UPDATE, НЕ двигает last_seen — иначе призрак «свежеет»); НОВ `reconcile.run_reachability_cycle(cfg,…)` — пингует known RFC1918-устройства (`scan.host_is_alive`, read-only TCP+1 SNMP-GET, RFC1918-гейт ПЕРЕД пробой + re-gate в scan), `build_graph`+`correlate` (roots = dev_type∈{agent,router}), `set_status` down/unreachable, ОДИН `root_cause` net_change на upstream, recovered→up; gated `cfg.enabled`, shared `_poll_lock`. `config.reachability_interval_sec` (default 600, floor 60). `main._netdisco_reachability_loop` (+`_run_netdisco_reachability_cycle` self-guard) в lifespan под `netdisco_enabled`. 28 P10-тестов (graph 6, correlation 4, changes 7, reconcile +6 [diff/lifecycle/reachability], db +1, config +1, loops +2; netdisco всего 189). CHANGELOG RU (Added: корень сбоя + журнал изменений). Gate green: ruff/format/mypy[103]/bandit clean, cov **92.75%**, smoke OK. **Security-review (Opus)=APPROVE** (0 crit/high/med/low; SQL `set_net_device_status`/`store_net_change` параметризованы, no-last_seen-side-effect; reachability bounded к inventory [нельзя увести на public/arbitrary range], read-only [нет SET PDU 0xA3], RFC1918 double-gate, self-guard+lock; no-demote [статусы через set_status не upsert]; graph BFS iterative+visited → нет бескон. рекурсии/циклов; correlate не фабрикует DOWN; diff nid-keyed). **Ledger-note (НЕ finding):** ARP-ip в net_devices без ingest-RFC1918-гейта, держит probe-time double-gate. **Для P11 (API + cache + telemetry, зависит P9/P10):** `netdisco/cache.py` `GraphCache` (TTL 30–60с, read-through, `threading.Lock`, инвалидация по новому snapshot id); `netdisco/metrics.py` thread-safe счётчики цикла (candidates/probes/snmp_ok/timeout/links/deltas/duration); `api.py` read-only `GET /topology/graph` (из кэша), `/topology/changes?days` (clamp days/limit на HTTP — P2 LOW), `/netdisco/devices/{id}` (интерфейсы+линки+reading-серия), `/netdisco/stats`; correlation read-side аннотация на device-странице (как `subnet_context_for`) + модулятор `network_risk`-confidence (blind-spot MEDIUM-cap, БЕЗ новых алармов — scope ceiling, reuse Score100/factors/reason). NB: дашборд-страница топологии = отдельная фаза/после; netmap уже есть.
- **NETDISCO Phase 9 — Data Fusion §4.4 + topology reconcile + persist links [merge `15e8b98` (no-ff; 3 коммита 697770c→420ec38, branch `feat/netdisco-phase9-fusion-reconcile`), 2026-06-22, ЗАПУШЕНО origin/main `43e4ed5..15e8b98`]:** улики P8 примиряются в граф рёбер и сохраняются. Новое: `netdisco/fusion.py` `fuse(evidence)→list[ResolvedLink]` (§4.4: `_node_id` нормализует endpoint → nid [MAC→`nd-mac-`, LLDP/CDP device-id→`nd-chassis-` через `identity.device_nid` slugify+cap48, готовый `nd-` passthrough], группировка по `frozenset({na,nb})`, self-link na==nb отброшен, winner=max по `(SOURCE_PRIORITY[source], observed_at, source)`, `SOURCE_PRIORITY={lldp:5,cdp:4,fdb_edge:3,route:3,arp:2,fdb_uplink:2,fdb_ambiguous:1}`, confidence=winner'а ∨ LOW при port-conflict, node-merge по chassis-id, детерм. сорт `(a,b,via_source)`; `_conflicted_ports`: порт `(na,if)` где top-priority источник видит >1 remote → ambiguous LOW обе [§4.4 «не выкидывать — показать обе»]); `models.ResolvedLink(a,b,via_source,confidence,link_kind=l2-edge,ambiguous,observed_at)` frozen, `link_kind=l2-trunk` для fdb_uplink. `netdisco/reconcile.py` `run_topology_cycle(cfg,…)` — gated `cfg.enabled`, shared `_poll_lock` busy-return, для каждого known router/switch/ap с RFC1918-ip (`is_rfc1918` ПЕРЕД сессией) read-only SnmpSession, `collect_evidence`→`fuse`→`db.replace_net_links([rows], probed_nids)` + `store_topology_snapshot(graph)` + `upsert_net_device(status=up)` advance last_seen; own-MAC фильтр через `get_net_device` (интерфейсы); replace-scope = ТОЛЬКО probed-узлы (линки непробленных не трогаются); deps инъектируемы. `db.replace_net_links(links,node_nids,received_at)` (НОВ) — идемпотентный per-node replace: SELECT текущих линков probed-узлов (IN-плейсхолдеры count-only, значения bound, `# nosec B608`), DELETE только исчезнувших (`current−new_keys`, `link_kind IS ?` NULL-safe), upsert текущих (canon a≤b, ON CONFLICT сохраняет first_seen) — ОДНА транзакция `_lock`; rerun не дублирует, чужие узлы не трогает. `config.topology_interval_sec` (default 3600, floor 60). `main._netdisco_topology_loop` (под `netdisco_enabled`, to_thread+interval+jitter, self-guard) + `_run_netdisco_topology_cycle`. 18 P9-тестов (fusion 8, reconcile 5, db 3, config 1, loops 2; netdisco всего 164). CHANGELOG RU (Added: карта L2-связей видна на карточке устройства). Gate green: ruff/format/mypy[100]/bandit clean, cov **92.83%**, smoke OK. **Security-review (Opus, ОБЯЗАТЕЛЬНЫЙ)=APPROVE** (0 crit/high/med; SQL: только count плейсхолдеров интерполируется, значения bound, DELETE+INSERT параметризованы, ОДНА транзакция [нет partial-write], идемпотент+scoped трассирован [first_seen preserved, чужие узлы не трогаются]; read-only [нет SET PDU 0xA3]; RFC1918-gate перед сессией; hostile LLDP/CDP device-id → `_norm_token` slugify alnum+cap48 [нет кавычек/SQL-метасимволов, в SQL только bound]; fuse pure/детерм.; no-demote upsert; loop self-guard). **1 LOW применён (`420ec38`):** fuse winner tie-break += source-строка (детерминизм для будущих route/arp равного приоритета). **Для P10 (Graph + reachability + changes, зависит от P9):** `netdisco/graph.py` `build_graph(devices,links)`/`neighbors`/`reachable_from(roots)` BFS/`path_to_root`/`status_rollup`/`find_root_cause(down_set)` (§3.5); `netdisco/correlation.py` `correlate(graph,down_set,roots)→{device:DOWN|UNREACHABLE|suppressed,root_cause}` (§1.5/§3.7: шлюз down → за-ним unreachable+suppressed+1 root-cause; confidence-cap MEDIUM при неполном графе — blind-spot НЕ тревога, D5); `netdisco/changes.py` `diff(prev_snapshot,curr)→list[TopologyDelta]` (appeared/disappeared/link±/iface-down/reclassified) + lifecycle `missing`(N-cycle устойчивость, ghost-lesson — НЕ по одному промаху)→`eligible_purge`(PURGE_AGE), пишет `net_changes`; DHCP-смена IP ≠ appeared/disappeared (identity `device_nid` chassis>sn>mac>ip уже стабилен); deters; wire в `reconcile` после snapshot + `_netdisco_reachability_loop`.
- **NETDISCO Phase 8 — LLDP/CDP/FDB evidence + L2-link inference §4.3 [merge `ec509dc` (no-ff; 3 коммита 87ddb39→aa6924c, branch `feat/netdisco-phase8-evidence-l2`), 2026-06-22, ЗАПУШЕНО origin/main `9ef1c35..ec509dc`]:** топология = граф улик (evidence), связь L2 не «факт», а вывод (LLDP>CDP>FDB). Новое: `netdisco/evidence.py` — `LinkEvidence(a,b,source,confidence,local_if,observed_at)` frozen (observed_at ставит topology-цикл, НЕ коллектор → коллекторы детерминированы); `collect_lldp(local,session)` walk `lldpRemChassisId` (1.0.8802.1.1.2.1.4.1.1.5; local-port в индексе TimeMark.LocalPortNum.RemIndex) → сосед HIGH, 6-октетный chassis рендерится как нормализ. MAC (склейка с FDB/ARP в P9), иначе printable-ASCII id; `collect_cdp(local,session)` walk `cdpCacheDeviceId` (1.3.6.1.4.1.9.9.23.1.2.1.1.6; local-ifIndex в индексе) → Cisco-сосед HIGH; `read_fdb(session)` → (`{bridge_port:{mac}}`,`{bridge_port:ifindex}`) из `dot1dTpFdbPort` (MAC в OID-суффиксе=6 decimal-октетов) + `dot1dBasePortIfIndex` (RAW-парс, фильтрация/мультикаст в l2; bridge_port<1 отброшен); `collect_evidence(device,session,*,infra_macs)` = LLDP+CDP+`l2.infer_edges(read_fdb)`, фильтрует own-MAC устройства (lazy-import l2 рвёт цикл evidence↔l2). `netdisco/l2.py` — `infer_edges(local,port_macs,port_ifindex,*,infra_macs,own_macs)` §4.3: macs порта минус мультикаст/broadcast/own → 1 не-инфра=**EDGE HIGH**; infra-MAC ∨ >`UPLINK_MAC_THRESHOLD`(4)=**UPLINK** (только инфра-MAC именуются LOW; безымянный транк→`[]`, не плодит ложный edge — UNKNOWN-first); 2..thr=**AMBIGUOUS** LOW per host; вывод сортирован (порядок входа не влияет; tie-break b,source,ifindex,bridge_port). `oids` += LLDP_REM_CHASSIS_ID/PORT_ID, CDP_CACHE_DEVICE_ID/PORT, DOT1D_BASE_PORT_IF_INDEX, DOT1D_STP_PORT_DESIGNATED_BRIDGE (STP — для P9 uplink-развязки «кто выше»). 16 тестов (evidence 8, l2 8). **БЕЗ caller'а** (нет цикла/persist/endpoint) → нет видимого поведения → нет CHANGELOG (правило «чисто внутреннее можно не писать»; видимую строку даст P9). Gate green: ruff/format/mypy[98]/bandit clean, cov **92.82%**, smoke OK. **Security-review (Opus, ОБЯЗАТЕЛЬНЫЙ)=APPROVE** (0 crit/high; read-only [SET не существует в SnmpSession], fail-closed parse трассирован+фаззинг [BER→int/str/None, OID-арки=неотрицат.decimal; `_mac_from_oid_octets` len6+0..255, `_chassis_id`/`_device_id_text` int→None, `_suffix_parts`/`_is_multicast` без raise → ноль исключений на гигантских арках/чужих тегах/сурогатах], нет SQL/eval/format-инъекций/секретов/egress, bounded walk[512], infer_edges линеен, детерминизм, транк без инфра-MAC не даёт ложный edge). **2 LOW применены (`aa6924c`):** read_fdb отбрасывает bridge_port<1; infer_edges сорт += bridge_port tie-break. **Для P9 (Data Fusion §4.4 + reconcile + persist, зависит от P8):** `netdisco/fusion.py` `fuse(evidence)→list[ResolvedLink(a,b,confidence,via_source,observed_at)]` (группировка по `frozenset({node_a,node_b})`, `SOURCE_PRIORITY={lldp:5,cdp:4,fdb_edge:3,route:3,arp:2,fdb_ambiguous:1}` [uplink — отдельный candidate], winner=max(priority,freshness), confidence=f(источник,согласованность,свежесть), node-merge по chassis-id, конфликт «1 порт↔2 соседа равных источников»→LOW+ambiguous обе показать, детерм. tie-break (priority,observed_at,sorted node-ids)); `netdisco/reconcile.py` `run_topology_cycle` — для known router/switch строит probe-сессию (как P6/P7), `collect_evidence`→`fuse`→`db.replace_net_links`(НОВ writer: идемпотентный full-replace, rerun не плодит дубли) + `store_topology_snapshot` + upsert devices; observed_at ставит цикл; петля `_netdisco_topology_loop` (под `netdisco_enabled`, to_thread+interval+jitter, на `_poll_lock`). Детерминизм: те же улики в любом порядке → идентичный граф.
- **NETDISCO Phase 7 — passive SNMP ARP/route harvest [merge `bbc6970` (no-ff; 4 коммита bc6c94b→1cb99e8, branch `feat/netdisco-phase7-passive-harvest`), 2026-06-21, ЗАПУШЕНО origin/main `ff8ea6c..bbc6970`]:** в цикле активного обнаружения сервер пассивно обходит уже известные роутеры/свитчи по SNMP (только чтение) и достаёт их таблицы соседей/маршрутов → новые адреса без пинга. Новое: `netdisco/harvest.py` — `harvest_arp(session)` walk `ipNetToMediaPhysAddress` (1.3.6.1.2.1.4.22.1.2) → [(ip,mac)] (IP из суффикса OID = ifIndex.a.b.c.d, MAC из значения через shared `snmp_probe._mac`), `harvest_routes(session)` walk `ipCidrRouteIfIndex` (1.3.6.1.2.1.4.24.4.1.5 — индекс кодирует dest.mask.tos.nextHop, один walk даёт всё) → [(cidr,next_hop,ifindex)]; ТОЛЬКО RFC1918 результаты (публичные сосед/next-hop отброшены), bounded walk (cap 4096), dedup, fail-closed (parse мусора→[], не бросает). `oids` += `IP_NET_TO_MEDIA_PHYS`/`IP_CIDR_ROUTE_IF_INDEX`. `discovery.gather_candidates` += `harvest_arp=[(ip,mac)]` источник (новый RFC1918-адрес → `snmp-arp`-кандидат, dedup против ARP/static/scan; is_rfc1918 recheck). `scheduler._harvest_infra` — для каждого known router/switch с RFC1918-ip строит сессию, harvest_arp+harvest_routes, next-hop'ы как `(nh,None)`; вшит в `run_discovery_cycle` (за `active_scan`-гейтом, на `_poll_lock`, session_factory/harvest-fns инъектируемы); route next-hops + соседи → кандидаты. 17 netdisco-тестов (harvest 8, discovery +2, scheduler +1, + дефенсив-ветки). CHANGELOG RU (Added). Gate green: ruff/format/mypy[96]/bandit clean, cov **92.64%** (discovery 100%, harvest 90%), smoke OK. **Security-review (Opus, ОБЯЗАТЕЛЬНЫЙ)=APPROVE** (0 crit/high/med; triple RFC1918-гейт [emit+probe-target+candidate-recheck], read-only GETNEXT [нет SET], untrusted-parse fail-closed — суффикс-индекс-математика трассирована на short/exact/over-length/out-of-range → нет IndexError/OOB/wrong-value, `_cidr` ловит NetmaskValueError, `_mac` 6-октет-only, snmp.parse_response→{} на мусоре; bounded 4096-walk + self-terminate; single-flight+double-gate; no-demotion). **Carry-forward (НЕ блок):** LOW-1 `scheduler.py` маппит route → `(next_hop, None)`, отбрасывая `cidr`/`ifindex` (write-only L3-улика до topology-фазы); LOW-2 аннотация nullability (не дефект). **Для P8 (LLDP/CDP/FDB evidence + L2-inference §4.3):** `netdisco/evidence.py` `LinkEvidence(a,b,source,confidence,observed_at)` frozen + EvidenceCollector (LLDP `lldpRemTable` 1.0.8802.1.1.2.1.4.1.1 авторитет высший > CDP `cdpCacheTable` 1.3.6.1.4.1.9.9.23 > FDB-вывод); `netdisco/l2.py` FDB-алгоритм §4.3 (port→set(MAC) минус мультикаст/own; ∩infra-MAC или >UPLINK_MAC_THRESHOLD=4 → uplink/trunk-кандидат; ==1 → EDGE-линк HIGH; 2..thr → ambiguous LOW); `oids` += LLDP/CDP/FDB/STP.
- **NETDISCO Phase 6 — SNMP probe + device classification [merge `cfebc15` (no-ff; 5 коммитов c917186→8b4bf50, branch `feat/netdisco-phase6-snmp-classify`), 2026-06-20, НЕ ЗАПУШЕНО]:** серверный фоновый цикл опрашивает уже известные RFC1918-хосты по SNMP (reuse `printers.snmp.SnmpSession`, ТОЛЬКО чтение GET/GETNEXT, SET не существует) и определяет тип. Новое: `netdisco/oids.py` (numeric OID: system-скаляры/ipForwarding/dot1dBaseBridgeAddress+FDB/ifTable-колонки/entPhysicalSerial/Printer-MIB root; `SYS_SERVICES_L2/L3` биты; `IF_TYPE_IEEE80211=71`), `netdisco/snmp_probe.py` `probe_device(ip, session)→DeviceProfile` (1 GET 6 скаляров; SNMP-немой→`responded=False` БЕЗ единого walk — мёртвый хост платит 1 таймаут; 5 walk ifTable-колонок + bounded presence walk'и FDB=8/Printer-MIB=8/serial=32; MAC из ifPhysAddress через latin-1 re-encode — только чистые 6 октетов, utf-8-mangled→None=UNKNOWN; `is_printer` reuse на bounded Printer-MIB walk; ifType 71 + MACs из того же ifTable-walk → AP/agent-детект без лишних round-trip), `netdisco/classify.py` `classify(profile, agent_macs)→dev_type` (§4.2: agent>router(ipForwarding=1)>switch(bridge+FDB)/ap(+ieee80211 iface)>printer>endpoint>unknown; НИКОГДА не читает sys_object_id — вендор-OID сам по себе НЕ тип; bridge без FDB→endpoint честно; немой хост с inventory-MAC→endpoint, без MAC→unknown), `netdisco/drivers/{__init__,standard}` (`select_driver(sys_object_id)` по PEN-вендору reuse `printers.oids.vendor_for_sysobjectid`, fallback standard; VENDOR_OIDS пусты до hardware-verify — seam для вендор-драйверов). `models.DeviceProfile` (frozen, UNKNOWN-first). `scheduler.run_classify_cycle(cfg)` — gated `cfg.enabled` (unicast к известным хостам, НЕ range-scan → active_scan-гейт НЕ нужен, это правильно), на shared `_poll_lock` (busy-return), фильтр `is_rfc1918(ip)` ПЕРЕД пробой, skip своих агентов (mac∈agent_macs) + уже классиф. инфра (`_NEEDS_CLASSIFY={unknown,endpoint}` ∨ status=discovered → не реклассифицирует/не демотит), inventory-MAC вливается в profile перед classify, driver-extras override model/serial; deps инъектируемы. `config.classify_interval_sec` (default 3600, floor-clamp≥60). `main.py` `_netdisco_classify_loop` (under `netdisco_enabled`, to_thread+interval+jitter, self-guard swallow+log). `SnmpSession.walk` += additive `max_rows` (read-only, только ужесточает; default 512 для принтер-вызывающих байт-в-байт). 67 netdisco-тестов (snmp_probe 14, classify 11, drivers 4, +config/scheduler/loops). CHANGELOG RU-строка (Added). Gate green: ruff/format/mypy[95]/bandit clean, pytest cov **92.68%** (classify/drivers 100%, scheduler 98%, snmp_probe 92%), smoke OK. **Security-review (Opus, ОБЯЗАТЕЛЬНЫЙ)=APPROVE** (0 crit/high/med; RFC1918-egress defense-in-depth [cycle-фильтр + config + gethostbyname-match], read-only [нет SET], parameterized SQL [upsert/store_interfaces], hostile-input [`_mac` brute-forced 1.77M комбо → НЕТ wrong-confident MAC], self-probe-guard, bounded+self-guarded, no-demotion, корректный double-gate). **Carry-forward (НЕ блок):** LOW-1 `_fleet_agent_macs`→приватный `netmap._agent_macs` (дубль в `inventory.py`) → публичный `agent_macs(snapshots)`; LOW-2 ifTable 5×walk(512). **Для P7 (passive harvest):** `netdisco/harvest.py` `harvest_arp(session)`/`harvest_routes(session)` (RFC1918-фильтр результатов, cap max_rows), влить в `gather_candidates`, `run_discovery_cycle` зовёт harvest для type∈{router,switch}.
- **NETDISCO Phase 5 — generalized active LAN scan [merge `c2b5c82`, 2026-06-20, ЗАПУШЕНО origin/main]:** активное обнаружение за ДВОЙНЫМ гейтом (`netdisco_enabled` ∧ `active_scan`, оба OFF по умолчанию; deployed `config.json` НЕ включает — нет блока `netdisco`). `netdisco/config.py` += scan-поля (`active_scan` (`is True`), `static_ips`/`scan_cidrs` RFC1918-фильтр на загрузке, `scan_max_hosts`≥0 (0=kill), `scan_workers` clamp[1,256], `scan_ports` range-check 1..65535/dedup/fallback, `snmp_community`/`snmp_version`(0/1), `discovery_interval_sec` clamp≥60). `netdisco/scan.py` (НОВ) — `host_is_alive(ip,*,ports,…)` (**RFC1918 short-circuit ПЕРЕД любым сокетом** → TCP-connect к ports → 1 read SNMP GET sysObjectID fallback) + `scan(cfg,host_check=…)`→[] unless active_scan; переиспользует `printers.scan.expand_cidrs/local_cidrs` (один источник RFC1918-перечисления), read-only, host/worker caps, injectable. `netdisco/discovery.py` (НОВ) — `gather_candidates(*,arp_snapshots,static_ips,scan_ips)`→list[NetCandidate], тонкий адаптер над `printers.discovery.merge` (MAC>IP dedup, RFC1918 recheck, без spooler). `scheduler.run_discovery_cycle(cfg,…)` — gated `active_scan`, на shared `_poll_lock`; апсертит ТОЛЬКО новые nid (skip known → **скан не демотит классиф. устройство**), UNKNOWN-first (`unknown` если scan-only без MAC, `endpoint` если MAC; status=`discovered`), deps injectable. `main.py` `_netdisco_discovery_loop` (double-gated в lifespan, to_thread+interval+jitter, self-guard swallow+log). `api.py` `/discovery/poll` += rate-limit (`check_rate_limit("endpoint:discovery_poll")` ПЕРЕД работой → 429 на flood; **P4 carry-forward закрыт**). 29 тестов (config +10, scan 8, discovery 4, scheduler +4, loops 2, api +1), 7 коммитов (config→scan→discovery→cycle→loop→api-guard→sec-fix). CHANGELOG RU-строка. Gate green (ruff/mypy[90]/bandit, cov **92.77%**, smoke OK). **Security-review (Opus)=APPROVE** (0 crit/high; RFC1918 airtight 4 слоя с pre-network short-circuit; read-only no-SET (snmp_set не существует); double-gate не включён в deployed; bounded; no-demotion + parameterized SQL; rate-limit-before-work). M2 (namespace rate-limit key) применён `416d9b7`. **Carry-forward (НЕ блок):** M1 пустой `scan_cidrs`→авто-/24 (RFC1918-safe, как у принтеров); M3 SNMP community cleartext `public` (присуще v1/v2c → P13 DPAPI). **Для P6 (SNMP probe+classify, security-review ОБЯЗАТЕЛЕН):** `netdisco/oids.py` (sysObjectID/sysServices/ipForwarding/dot1dBaseBridgeAddress/ifTable/entPhysicalSerial, numeric); `netdisco/snmp_probe.py` `probe_device(ip,session)→DeviceProfile` (reuse `printers.snmp.SnmpSession`, read-only); `netdisco/classify.py` `classify(profile,agent_macs)→dev_type` (router по ipForwarding, switch по bridge-MIB+FDB, printer reuse, UNKNOWN-first, vendor-OID сам по себе НЕ тип); `netdisco/drivers/{__init__,standard}.py` (OID-карты пустые до hardware-verify); `scheduler.run_classify_cycle`+loop. Классиф. перезапишет P5-устройства (`discovered`/`unknown`) — `upsert_net_device` CASE keep-known-over-`unknown` это допускает.
- **NETDISCO Phase 4 — live inventory loop + force-poll [merge `763dbd9`, 2026-06-20]:** `netdisco/config.py` `NetdiscoConfig`(frozen: `enabled=False`/`inventory_interval_sec`≥60 clamp/`jitter_sec`)+`load_netdisco_config`; `ServerConfig` += `netdisco_enabled=False`/`netdisco`/`retain_net_readings`/`retain_net_snapshots`+`netdisco_config()`. `netdisco/scheduler.py` `run_inventory_cycle(get_snapshots,upsert инъекции; `_poll_lock` non-blocking acquire, release в finally → `busy` при гонке)`+`poll_now()`. `main.py` lifespan: `_run_netdisco_cycle`(swallow+log)+`_netdisco_loop`(to_thread+`sleep(interval+random.uniform(0,jitter))` `# nosec B311`) под `if cfg.netdisco_enabled`; `db.init_db(...,retain_net_*)`; `app.state.netdisco_config`. `POST /api/v1/discovery/poll` (force, делегирует `poll_now`). **OFF-by-default → zero-regression** (smoke boots). 15 тестов (config 7 + scheduler 2 + api 6), 4 коммита (config→scheduler→loop→api). CHANGELOG обновлён. Gate green (cov **92.84%**, smoke OK). Code-review (Sonnet)=**APPROVE** (0 crit/high/med). **2 LOW carry-forward:** (1) `/discovery/poll` БЕЗ auth/rate-limit (наследие `/printers/poll`) — ок пока цикл дешёвый (rebuild из snapshots), НО **перед тем как P5 повесит активный скан за этот lock — добавить rate-limit/guard на force-poll**; (2) loop+app.state парсят config дважды (косметика, как у принтеров). **Для P5 (active scan, ЗА `active_scan` stop-gate, security-review ОБЯЗАТЕЛЕН Opus):** `netdisco/scan.py` = обобщить `printers/scan.py` (`host_is_alive`=TCP-connect к `scan_ports` ∪ SNMP-ping sysObjectID; RFC1918 double-check; cap hosts/workers; kill-switch `max_hosts=0`; injectable host_check); `netdisco/discovery.py` `gather_candidates`(обобщить `printers/discovery.merge`); `NetdiscoConfig` += `active_scan=False`/`scan_cidrs`(фильтр `is_rfc1918_cidr` из `printers/discovery`)/`scan_max_hosts`/`scan_ports`/parallelism caps; `run_discovery_cycle`+loop; новые кандидаты → `upsert_net_device(dev_type=endpoint, sources+=scan)`; + guard на force-poll.
- **NETDISCO Phase 3 — persistent inventory + API [merge `ee08e23`, 2026-06-20]:** `netdisco/inventory.py` — `build_inventory(snapshots)` (ЧИСТАЯ) из `db.get_network_snapshots()`: агенты по их adapter-MAC → `dev_type=agent` (identity-layer: сосед с MAC известного агента НЕ дублируется как endpoint), безагентные ARP-соседи → `endpoint` (есть MAC) / `unknown` (нет), вендор через `oui.vendor_for_mac`, identity `device_nid`, dedup по nid (один шлюз у N агентов = одно устройство), детерминированный sorted-вывод, COALESCE-слияние (known не затирается None); `persist_inventory(devices, upsert=db.upsert_net_device)` (инъекция для тестов; last_seen ставит сервер, sources транзиентны). Новый read-only `GET /api/v1/netdisco/devices?dev_type=&site=` (делегирует Phase-2 getter, без инъекций). БЕЗ новой сети/агента/контракта. 11 тестов (`test_netdisco_inventory.py` 8 + `test_netdisco_api.py` 4? итого 11), 4 коммита (build→persist→API+CHANGELOG→review-fix). CHANGELOG-строка (Added). Gate green (cov **93.07%**, smoke OK). Code-review (Sonnet)=APPROVE-WITH-FIXES → MEDIUM (raw MAC в `device_nid` соседа → нормализованный) исправлен + site-test + комментарий persist. НЕ запушено. **Для P4:** `netdisco/config.py` `NetdiscoConfig`(frozen, `enabled=False`, `inventory_interval_sec` clamp≥60, `jitter_sec`)+`load_netdisco_config`; `ServerConfig` += `netdisco_enabled=False`/`netdisco=None`/`retain_net_*`/`netdisco_config()` (зеркало `printer_*`); `netdisco/scheduler.py` `run_inventory_cycle`(get_snapshots/upsert инъекции, `_poll_lock` anti-DoS)+`poll_now`; `main.py` lifespan `_netdisco_loop` (как `_printer_poll_loop`: to_thread+sleep+jitter, под `if cfg.netdisco_enabled`)+`db.init_db(...,retain_net_*)`+`app.state.netdisco_config`; `POST /api/v1/discovery/poll` (force, bounded, как `/printers/poll`).
- **NETDISCO Phase 2 — topology storage [merge `75942f3`, 2026-06-20]:** `db.py` += таблицы `net_devices`(PK `device_nid`, COALESCE-инвентарь)/`net_interfaces`/`net_links`(canon a_nid≤b_nid + `UNIQUE(a,b,link_kind)`)/`net_device_readings`(append-only)/`net_topology_snapshots`(append-only граф)/`net_changes`(журнал) — все `IF NOT EXISTS` (идемпотентная миграция). Writers: `upsert_net_device` (COALESCE identity; `dev_type` CASE keep-known-over-`unknown`; `status` COALESCE; first_seen preserve/last_seen advance), `store_net_device_reading`+`store_topology_snapshot` (append+prune top-N by id), `store_net_interfaces` (full-replace per device), `upsert_net_link` (canon-порядок+swap ifs, a_if/b_if COALESCE, via_source/confidence latest-wins), `store_net_change`. Getters: `get_net_devices(dev_type,site)` (Python-фильтр, без SQL-строкосборки), `get_net_device(nid)` (+interfaces+links), `get_net_links`, `get_latest_topology_snapshot`, `get_net_changes(days,limit)` (параметризованный ISO-cutoff+LIMIT). `init_db` += `retain_net_readings=2000`/`retain_net_snapshots=500` (globals `_retain_net`/`_retain_net_topo`). **net_* НЕ в `_DEVICE_TABLES`**. 14 тестов `test_netdisco_db.py`, 3 коммита (schema→writers→getters). Gate green (cov **93.09%**, smoke OK). Security-review (Opus)=**APPROVE** (0 crit/high/med). LOW→P11: клампить `limit`/`days` на HTTP-границе. НЕ запушено. **Для P3:** `netdisco/inventory.py` — `build_inventory(db.get_network_snapshots())`→list[NetDevice] (агенты по adapter-MAC→`agent`; ARP-соседи без агента→`endpoint`/`unknown` через `oui.vendor_for_mac`; identity `device_nid`; снап-shape `{device_id,hostname,site_code,last_seen,adapters[],neighbors[],quality[]}` — см. `netmap._agent_macs`)→`persist_inventory`→`upsert_net_device`; read-only `GET /api/v1/netdisco/devices`. Без новой сети/агента/контракта.
- **NETDISCO Phase 1 — identity + models [merge `bc54833`, 2026-06-20]:** новый пакет `server/netdisco/` (обобщение принтерного движка под всю сеть; RFC `docs/superpowers/specs/2026-06-20-network-discovery-rfc.md` + план 13 фаз `…/plans/2026-06-20-network-discovery-plan.md`; память `[[project-netdisco]]`). Реализовано: `identity.device_nid(*, chassis_id, serial, mac, ip)` (precedence chassis>serial>mac>ip; MAC через `analytics.oui.normalize_mac`; ip-fallback валидируется stdlib `ipaddress` — мусор/нерезолв→`nd-unknown`; DHCP-стабильность: тот же MAC→тот же id) + `merge_identity(old,new)` (сильнейшая схема побеждает, слабая/равная не демотирует); `models.NetDevice/NetInterface` (frozen, UNKNOWN→None/()). Чисто, без БД/сети/wiring. TDD 15 тестов (`tests/test_netdisco_identity.py`+`_models.py`, RED→GREEN), 3 коммита (models→identity→review-fix). Gate green (ruff/mypy/bandit clean, cov **93.07%**, smoke OK). Code-review (Sonnet)=APPROVE-WITH-FIXES → MEDIUM (ip не валидировался, nid=будущий ключ) исправлен `6d5e75d`; 2 LOW приняты (`prn-mac-*` vs `nd-mac-*` namespaces намеренно несовместимы; `merge_identity` мусор→keep-old). НЕ запушено (push по команде). **Для P2 помнить:** добавить в `db.py` `_SCHEMA` таблицы `net_devices`(PK device_nid)/`net_interfaces`/`net_links`(canon a_nid≤b_nid + UNIQUE(a,b,kind))/`net_device_readings`/`net_topology_snapshots`/`net_changes` — зеркало `store_printer_reading` (INSERT reading→prune top-N by id→`INSERT…ON CONFLICT DO UPDATE` COALESCE identity, status latest-wins, first_seen preserve/last_seen advance); `dev_type` upsert = CASE keep-known-over-`unknown`; net_* НЕ в `_DEVICE_TABLES` (иная identity, свой lifecycle); retain-параметры в `init_db` как `_retain_prn`.
- **Дашборд флота — 5 правок UI (branch `feat/dashboard-ip-sort-noredark`, 2026-06-20, user «go ahead смерджи и запушь»):** (1) `fmt_age` «только что»→«0с» + liveinfo строки; (2) заголовок «Устройство» теперь `sortable data-sort="text"`, JS-сорт по `data-name` (localeCompare ru); (3) новый столбец **IP** (после «Связь») — `db._primary_ip()` берёт первый ipv4 первого адаптера из последнего historical payload (RFC1918, display-only, cap ≤64); клик по `.ip-copy` копирует в буфер (clipboard API + execCommand fallback, подсветка `.copied`); IP экранирован (autoescape SSR, только в data-ip/clipboard → XSS-safe); (4) тренд-стрелка ↑N = контрастный highlight (800/16px, bg `var(--bad)` + чёрный текст); (5) **красный убран из тёмной темы полностью** — `:root --bad`/`--bad-dim` → жёлтый `#ffe000` highlight (флипает все токен-красные на dark); fleet.html хардкод-красные → токен; светлые темы (meta/vercel) красный сохранён. +5 тестов `test_live_dashboard.py`. Gate green: ruff/mypy/bandit ✓, cov **92.96%**, smoke OK.
- **КЛОН-БЕЗОПАСНЫЙ device_id — gate green, branch `fix/device-id-clone-safe` (2026-06-15, user-directed «вариант 3, merge+push»):** баг — машины-клоны из одного образа диска (Sysprep `/generalize` пропущен) делят реестровый `MachineGuid`, а `device_id` брался прямо из него → на сервере `device_id` = PRIMARY KEY → клоны затирали друг друга, во флоте видно <10 устройств вместо 15+. Фикс: чистая функция `resolve_device_id(machine_guid, hostname)` в `client/config.py` — `device_id = "dev-"+sha256("srp-device-v1|"+guid+"|"+hostname)[:24]`; клоны с разными именами → разные ID; сырой GUID наружу не уходит (приватность как у хеша серийников); нет GUID (не-Windows) → прежний `agent-<uuid>`. **Только новые установки**: персистнутый `device_id` не пере-выводится (нет «переезда» здорового флота); уже схлопнувшиеся клоны = разовый ресет (очистить `device_id` в `config.json` + рестарт). Контракт НЕ меняется (`device_id: str`, bump не нужен). 8 новых тестов (16/16 green). Security-review (Opus) = **SAFE TO MERGE**. Gate green: ruff/mypy/bandit ✓, cov 94.28%, SMOKE OK. org_directory.json (чужой незакоммиченный diff) НЕ трогал.
- **Ф2 FOLLOW-UP контрактные капы — MERGED ДВАЖДЫ, ПРИМИРЕНО merge'ом origin/main (2026-06-12):** origin-линия `7b97305` (feat `bdd27ef`, 2026-06-11, БЫЛА ЗАПУШЕНА) + локальный re-commit `9cb1fb0` (feat `0a30e2e`, 2026-06-12); содержимое идентично, git слил чисто (конфликт только в ledger). История граблей: сессия 2026-06-11 закоммитила И запушила капы, но локальные коммиты были потеряны; сессия 2026-06-12 не проверила origin, объявила хэши «фантомными» и перекоммитила с диска. **Урок исправлен: перед вердиктом «коммит не существует» проверять `git log origin/main` / `--all`, а не только локальный лог.** Сами капы: `shared/schema.py` `NET_ADAPTERS_MAX=64/NET_NEIGHBORS_MAX=512/NET_CONNECTIONS_MAX=512/NET_QUALITY_MAX=16` как `Field(max_length=…)` на network-списках `HistoricalPayload` (раздутый payload → 422 на границе, не хранится); агент режет adapters/quality (кап агента ≤ контрактного, тест пинит); stale-комментарий `sources.py` «Phase 1 ungated» → «Phase 2 trust domain» (на origin отдельным merge `69b1cf7`). 10 тестов `test_contract_network_caps.py`. Gate green в обеих сессиях (cov 94.28%, smoke OK). Security-review (Opus) = **APPROVE** (обе стороны).
- **ЖИВАЯ КАРТА СЕТИ — MERGED `2fd9063` (feat `b4eb0fd` + review-fix `4a29960`, branch `feat/netmap-visual`, 2026-06-12, user-directed «карта должна работать и удивлять, без оверинжениринга»):** интерактивный force-граф на чистом canvas ПОВЕРХ SSR-страницы `/netmap` (SSR-таблицы сохранены как текстовая/доступная форма; меняли ТОЛЬКО `netmap.html` + тесты — сервер не тронут, данные через JSON-остров `<script type="application/json">{{ m|tojson }}</script>`). Фичи: шлюзы-ромбы (OUI-вендор, glow), агенты-круги цвета здоровья линка (good/warn/bad/na = пороги SSR-чипов), без-агентные ARP-устройства точками, **пульсы «пакетов» бегут по измеренным линкам и детерминированно гибнут на полпути при потерях** (bad→55%, warn→80%), аномальная подсеть = расходящиеся красные кольца на шлюзе, Wi-Fi линки пунктиром, drag узлов/панорама/zoom колесом/dblclick-сброс, поиск гасит непохожие узлы (тот же input что SSR-фильтр), тултипы только textContent, клик по агенту → `/device/{id}` (encodeURIComponent), детерминированная раскладка (mulberry32 от hash шлюза), все 3 темы через getComputedStyle+MutationObserver, prefers-reduced-motion = статичный кадр. Физика: пружины+отталкивание+гравитация к центру кластера, синхронная предстабилизация (адаптивно 140/300 тиков), построчная упаковка кластеров. 3 новых теста (canvas+JSON-остров round-trip, пустое состояние, **XSS-пин: hostname `</script><script>` не рвёт остров** — tojson экранирует). Code-review (Sonnet) = APPROVE-WITH-FIXES, оба HIGH исправлены `4a29960`: SAFE_CLS whitelist на классы тултипа; pick() ходит по тому же sorted-массиву что draw (верхний узел выигрывает, убран путаный early-return — ревьюер не заметил, что наивное удаление вернуло бы всегда null); hoisted sort из draw-кадра; адаптивный pre-sim. Проверено живьём (playwright, 3 подсети, 10 агентов): hover/фильтр/темы работают, console errors = none, скриншоты dark/meta. Gate green (cov 94.28%, SMOKE OK). НЕ запушено (push по команде).
- **NETWORK Ф2 (2026-06-11) — built on `feat/network-phase2`, gate green (ruff/mypy/bandit clean, cov 94.26%, SMOKE OK), in review→merge:** spec `docs/superpowers/specs/2026-06-10-network-phase2-design.md` (D1–D12) + plan `docs/superpowers/plans/2026-06-10-network-phase2.md`. Server-only (агент+контракт НЕ тронуты): (1) `server/analytics/oui.py` — seed OUI→vendor (только virtualization+RPi, 100% уверенность; UNKNOWN over invented vendor); (2) `server/analytics/netmap.py` — чистый строитель карты: кластеры по ШЛЮЗУ (Ф1 не даёт prefix/iface соседей), identity по нормализованному MAC (сосед с MAC агента = тот агент), union+dedup чужих узлов, шлюз выносится в заголовок кластера; **subnet anomaly D9**: ≥2 отчитавшихся И ≥60% с потерями ≥20% → «инфраструктура, не ПК»; `subnet_context_for()` = RU-аннотация устройства; **read-side only (D7, урок W4.0)** — никогда в recompute; (3) `db.get_network_snapshots()` — один fleet-запрос latest-by-id; (4) `server/analytics/network_risk.py` — ось `network_risk` (higher_is_worse, confidence cap MEDIUM D11): шлюз ведёт (loss 100%→+45 / ≥20→+30 / ≥5→+15; lat ≥100ms→+15 / ≥30→+8), APIPA standalone +35, Wi-Fi <30%→+12 / <50%→+6, DNS partial +8 once; **ICMP-честность D5**: все пробы без ответа → blind spot (не тревога), full-loss DNS при живом шлюзе игнорируется (lineage count); порядок гейтов: untrusted → нет-данных («нет сетевой телеметрии» для старых агентов) → domain gate-fail (withheld); (5) **trust flip**: `DOMAIN_SOURCES["network"]={required:[network]}`, `validate_network` range-checks (loss 0..100, lat 0..60000, signal 0..100), `_extract_reading` = quality+counts БЕЗ соседей/связей (last_good не раздувается); observability теперь 7 доменов (старый агент ≈85.7 — честное слепое пятно D12); day-1 health-оси НЕ зависят от network (тест прибит); (6) тренд `gateway_latency` direction-only (D10, не в _DEPLETION_DOMAINS — траекторию не выдумывает); (7) web: `/netmap` страница (SSR, без graph-JS; поиск vanilla; баннер аномалии) + `GET /api/v1/netmap` + nav «карта сети» + device.html: axis-card «Здоровье сети» (+ аннотация подсети D8 + blind spots) + таблица «Качество связи» («нет ответа» chip для ICMP-неоднозначности); (8) CHANGELOG RU-строка. Тесты: test_netmap.py (7), test_network_risk.py (14), test_netmap_web.py (4), test_network_ingest_trust.py переписан на Ф2-инварианты (8), +trend. Hook-grabli: PostToolUse срезает импорт, добавленный раньше использования — дважды восстанавливал (§5 правило подтверждено). **REVIEWED (dual subagent): security=APPROVE-WITH-FIXES (0 Critical/High; XSS/SQL/privacy/D7-write-path verified clean), code=APPROVE-WITH-FIXES (2 HIGH). Fixes applied `4e1bc26`:** (a) read-side caps в `get_network_snapshots` (adapters 64 / neighbors 512 / quality 16) — один раздутый payload не замедляет все страницы; (b) `seen_by` = УНИКАЛЬНЫЕ агенты (set device_id), не записи ARP; (c) `_gateway_quality` скоуплен (нет gw-пробы → None, явно); (d) `subnet_context_for` строит карту только по подсетям устройства (страница устройства не платит за чужие кластеры); (e) тренд-тест усилен изоляционным инвариантом (gateway_latency один → trajectory None). **FOLLOW-UP (контрактный PR, R4):** `max_length` на network_adapters/neighbors/quality в `shared/schema.py` — настоящий boundary-фикс (review #1). **Accepted-minor:** gateway_mac last-write-wins при ARP-конфликте (LOW; скрытый dedup, не тревога); фактор loss+latency без суммарного пояснения (doc-gap LOW); `_ip_key` сортирует «999.999…» как валидный (косметика). **MERGED to `main` @ `5e6c1fd` (no-ff, 15 commits, +2638/−23). PUSH не делал — жду команды.** Ф3 (активный скан, за гейтом) не начата.
- **W0.1 append-only longitudinal history MERGED to `main` (`a6f3de2`):** `historical`+`scores` latest-wins → append-only (`id` AUTOINCREMENT + `(device_id,id)` idx); `store_*` = INSERT + per-device retention prune (`_retain_hist=2000`/`_retain_scores=5000`); `get_historical`/`get_device` latest-by-id; `get_devices` fleet JOIN → `MAX(id)` per device (no row-multiplication); new `get_historical_series`/`get_score_series` (newest-first+limit); **atomic+idempotent in-place migration** of pre-W0.1 DBs (BEGIN/COMMIT rebuild + `DROP IF EXISTS …__new` shadow; lossless ≤1-row/device). 11 tests (`test_db_append_only.py`). Gate green (cov 91.18%, smoke OK); security-reviewed APPROVE-WITH-FIXES → HIGH (migration crash-window) **empirically DISPROVEN** (tx DDL rolls back DROP — verified by abort test), MEDIUM constant-table guard added (`_has_id_column`), MEDIUM "test fails" = FP (test calls `upsert_device`). Accepted-minor: retention = count-cap per device (downsample policy deferred, cctodo W0.1).
- **W0.2 server-stamped time + clock-drift (P0) — gate green, branch `feat/w0.2-received-at`, pending review+merge:** `pipeline.ingest_envelope` stamps `received_at` per envelope + signed `clock_drift_sec = received_at − env.ts`; threaded into `store_historical/heartbeat/events` + `upsert/touch_device` as **additive optional params** (direct db test-callers unbroken). `devices.last_seen` re-anchored to **server receipt** (staleness no longer trusts client clock); `last_reported_ts` keeps the client claim. New cols on historical/heartbeats/events (`received_at`,`clock_drift_sec`) + devices (`last_reported_ts`,`clock_drift_sec`) via additive `_migrate_add_columns` (ALTER + backfill `received_at=ts`/`last_reported_ts=last_seen`, idempotent, no-op on fresh, runs after `_SCHEMA`). Series (`get_recent_heartbeats`/`get_historical_series`) now expose `received_at` (W4 trend x-axis); new `count_events_since` windows by **receipt**; fleet + device surface `clock_drift`(bool >300s)/`clock_drift_sec`. `scores.ts` already server-time (untouched). 7 tests `test_clock_time.py` (RED→GREEN). Gate green, cov **90.91%**, smoke OK; security-reviewed APPROVE; **MERGED `403f3f7`.**
- **W0.4 CONTRACT_VERSION compat (P0) — gate green, branch `feat/w0.4-contract-compat`, pending merge:** added pure helpers `parse_version`/`is_contract_compatible` to `shared/schema.py` (same-MAJOR = compatible; unparseable/None → incompatible, flagged not trusted). `ingest_envelope` response now carries `server_contract_version` + `contract_compatible` (capability negotiation); a wrong-MAJOR agent is flagged but its telemetry is **still stored** (UNKNOWN-over-false-confidence — never drop data on version mismatch). No contract field added → **no CONTRACT_VERSION bump** (still `0.1.0`). 19 tests `test_contract_compat.py` (RED→GREEN, incl. HTTP fwd/back-compat). Gate green, cov **91.16%**, smoke OK; code-reviewed APPROVE; **MERGED `b58a166`.**
- **W0.5 confidence-gated Score100 (P0, LAST) — MERGED `341bbdb`; post-merge security-reviewed `bd8a61d` (both HIGH = FP verified; hardening applied: untrusted-observability low-conf+reason not high, zero-domain → UNKNOWN, named risk threshold `_RISK_LOW_CONF_OPTIONAL_UNKNOWN`, +2 tests for previously-uncovered paths; gate green cov 91.68%):** new `server/scoring/score100.py` = `Score100` envelope (`value 0..100|None · direction · band · confidence · factors · missing_evidence · source_lineage · reason`) + helpers `make_score100`/`band_for_health_score`/`band_for_risk_score`/`legacy_value`/`compute_observability_score`/`compute_day1_score100`. `recompute_scores` wraps day-1 numbers in the gated envelope (stored in `risk.score100`: performance/reliability/wear/risk_exposure/observability) and derives the legacy numeric columns via `legacy_value` (no DB schema churn). Gating: untrusted identity → all day-1 withheld (None/unknown); required domain UNKNOWN → health value withheld, risk kept at low confidence (blind-spot under-counts); old agent (no source_health) → values kept, confidence low + `source_health missing`; observability = domain coverage penalised by unknown/regressed/clock-drift/untrusted. Raw `compute_day1_scores` unchanged (pre-gating heuristic). 8 tests `test_score100.py` + updated `test_scores.py`/`test_trust_scoring.py`. Gate green, cov **91.62%**, smoke OK. **ALL P0 CLOSED on merge → next legitimate phase = W4 `server/analytics/` (robust→trajectory→cohort→hypotheses→queue) reusing Score100.**
- **W0.3 telemetry-trust Plans 1 + 2 + 3 (3a–3e) merged to `main` + pushed** (`main` @ `d13cc72`):
  - **Plan 1** (`server/trust/`, merge `73a2be2`): pure trust core (state/weight, collector⊥semantic, materiality, Tiered domains).
  - **Plan 2** (merge `0e1d3d0`): `SourceHealth`+`Envelope.source_health`; agent `run_ps`→`PsResult`; collectors emit per-source `collector_status`; stdlib-pure.
  - **Plan 3** (merge `1c77ce9`): DB last-good+trust+`device_source_trust`; `evaluate_trust` on ingest (per-source→domain trust, lineage, accumulates across msg types); risk gated by trust (UNKNOWN classes; untrusted device withholds day-1 scores §7); dashboard surfaces trust + coverage.
  - **Plan 3e** (merge `d13cc72`): newly-blocked detection — a source that delivered before and now fails is flagged `regressed` (vs never-seen), surfaced in lineage + dashboard banner. No schema change (derived from `source_last_good` + collector_status).
  - Gates green throughout (coverage 92%); each plan spec + code-quality reviewed via subagent.
- Docs on main: `cctodo.md`, `telemetry-trust-contract.md`, `telemetry-trust-plan.md`. Memory: `system-class-high-trust`, `telemetry-trust-rules`, `working-style-governors`. `.codegraph/` gitignored.

### Now
- **CURRENT (2026-06-19): СЕТЕВЫЕ ПРИНТЕРЫ — ИСПОЛНЕНИЕ ФАЗ 4→7 (user: «доделай фазы до конца, пушить и мерджить после каждой фазы»; ВТОРОЙ директив: «включить по умолчанию + РАЗРЕШИТЬ сетевое сканирование + опрос всего сегмента LAN периодически; каждую фазу писать в `.claude/memory/*.md`»).** ⚠ **СТОП-ГЕЙТ АКТИВНОГО СКАНА СНЯТ ПИСЬМЕННО пользователем 2026-06-19** — Фаза 7 (скан сегмента) теперь авторизована (своя сеть/принтеры; легитимная инвентаризация как PRTG/Zabbix). Рамки держим: ТОЛЬКО RFC1918, жёсткие капы хостов/таймауты/параллельность, порты 161/9100/631, SNMP read-only; предупредил про EDR/IDS-тревоги. **ФАЗА 4 (планировщик+БД) DONE, MERGED `fd88308` (no-ff; feat `4e34da1`, branch `feat/printers-phase4-scheduler-db`), PUSH далее.** Сделано: `server/printers/scheduler.py` (`run_poll_cycle` = ThreadPoolExecutor fan-out по кандидатам discovery, guard ВНУТРИ воркера — битый хост → лог+«error», цикл/старт/loop живут; `poll_now` = discovery.merge→cycle, server-glue, НИКОГДА не сканирует диапазоны — только уже известные хосты); `db` таблицы `printers` (latest inventory, upsert COALESCE идентичности+счётчика, status latest-wins, first_seen хранится) + `printer_readings` (append-only скаляры + JSON `detail` блоб, retention-cap на принтер), ключ = `printer_identity(serial>mac>ip)` (НЕ device_id → намеренно ВНЕ `_DEVICE_TABLES`, принтеры = общая инфра); `models.printer_identity`; `config.py` `printer_poll_enabled`(дефолт OFF=безопасно, но в `server/config.json` ВКЛ)+`printers`+`retain_printer_readings`+`printer_config()`; `main.py` lifespan-loop (poll-first, guarded, чистая отмена) стартует при `printer_poll_enabled`; standard-драйвер `_i` клампит int64 (review LOW-1). Gate: ruff/mypy[70]/bandit clean, cov **94.03%**, SMOKE OK. **Security-review (Opus, SQL/loop/ресурсы) = APPROVE** (7/7 инвариантов: SQL параметризован, нет публичного egress [RFC1918-фильтр в discovery держит], нет секретов, ThreadPool bounded+guarded, нет опасного sink, retention per-printer корректен, active-scan гейт цел; LOW-1 вкатан, LOW-2 limit-кламп = заметка для Фазы 6). Тесты: test_printer_db.py(9)/test_printer_scheduler.py(5)/printer_models +5. Память: будет `.claude/memory/printer-monitoring-arch.md` + `printer-active-scan-authorized.md`. **ФАЗА 5 (вендор-драйверы + IPP/HTTP fallback + каскад) DONE, MERGED `2a2fde4` (no-ff; feat `96e927b`, branch `feat/printers-phase5-vendor-ipp-http`), PUSH далее.** `ipp.py` (IPP Get-Printer-Attributes через urllib для принтеров с выключенным SNMP → модель/состояние; RFC1918-only, **no-redirect opener = SSRF-гард**, таймаут, кап ответа), `http_probe.py` (веб-рут → модель из `<title>` за printer-hint-гейтом, bounded regex, БЕЗ выдуманных счётчиков), `collector.probe` каскад SNMP→IPP→HTTP (первый опознавший побеждает; fallback'и инъектируемы для тестов), `drivers/vendor.py`+8 вендор-драйверов (overlay цвет/ч-б ТОЛЬКО supplementary; стандартный total остаётся авторитетным; absent OID→None; `oids.VENDOR` карты ПУСТЫЕ до верификации на железе — честность > выдуманные OID). Gate: ruff/mypy[81]/bandit clean, cov **93.90%**, SMOKE OK. **Security-review (Opus, SSRF/urllib/недоверенный парсинг) = APPROVE-WITH-FIXES, все применены** (HIGH no-redirect opener против SSRF-через-redirect; MED bounded title-regex против backtracking; LOW общий int64-клампинг). Память: `printer-ipp-http-fallback.md`. **ФАЗА 6 (дашборд `/printers`) DONE, MERGED `978e9d9` (no-ff; feat `05a655e`, branch `feat/printers-phase6-dashboard`), PUSH далее.** `/printers` (список: модель/IP/вендор/статус/HW-счётчик/расходник%/ошибки/даты/источник + поиск + Plotly-бар + кнопка «Опросить сейчас») и `/printers/{id}` (история счётчика + расходники/лотки/ошибки + с каких ПК печатали); API `GET /printers`,`/printers/{id}`,`POST /printers/poll`; `db.get_printers_overview/get_printer_detail/get_printer_print_summary` + `_match_software` (HW↔SW сверка: whole-token IP/hostname/model, НЕ в trust/scoring; несопоставленное — отдельным списком); принтеры на «карте сети» через `_attach_printers_to_netmap` (web-слой; чистый `build_netmap` НЕ тронут) — SSR-сетки + новый кликабельный canvas-узел; `poll_now` сериализован `_poll_lock` (анти-DoS). XSS: SSR autoescape + srpEsc на Plotly/JS + tojson-острова, пин-тесты на обеих страницах. Gate: ruff/mypy[81]/bandit clean, cov **93.96%**, SMOKE OK. **Security-review (Opus, XSS/SQL/poll) = APPROVE-WITH-FIXES, применены** (MED `_poll_lock` in-flight, LOW whole-token IP, LOW detail-XSS-тест). Память `printer-dashboard-reconcile.md`. **ЦЕЛЬ ЮЗЕРА ПО ДАШБОРДУ ЗАКРЫТА** (страницы/счётчики/IP/компы-источники/даты + фоновый+принудительный опрос + принтеры на карте). **ФАЗА 7 (активный скан сегмента LAN, owner-authorized) DONE, MERGED `1894e9d` (no-ff; feat `10883af`, branch `feat/printers-phase7-active-scan`), PUSH далее. ВСЕ 7 ФАЗ ПРИНТЕРОВ ЗАКРЫТЫ.** `server/printers/scan.py`: `local_cidrs()` (авто-/24 сервера = «сегмент»), `expand_cidrs()` (RFC1918-only, dedup, кап `scan_max_hosts`=4096, `0`=kill-switch), `host_is_candidate()` (TCP 9100/631 → read SNMP GET 161), `scan()` ([] без active_scan; ThreadPool; host_check инъектируем). `discovery.is_rfc1918_cidr`+`merge(scan_ips=)`; `config` `scan_cidrs`(RFC1918-CIDR-фильтр)+`scan_max_hosts`; `scheduler.poll_now` зовёт scan ТОЛЬКО при active_scan. `server/config.json` active_scan=true (директива владельца). Рамки в КОДЕ: RFC1918 на 5 слоях (публичный/link-local/CGNAT/IPv6 пакет невозможен), кап хостов, bounded parallelism, короткие таймауты, только принтер-порты, без SNMP SET; code-default False → тесты НЕ сканируют (пробы инъектированы). Gate: ruff/mypy[82]/bandit clean, cov **92.89%**, SMOKE OK. **Security-review (Opus, обязательный) = APPROVE-WITH-FIXES, применены** (LOW max_hosts≤0 kill-switch, LOW broaden worker guard); EDR/IDS-предупреждение в CHANGELOG. Память `[[printer-active-scan-authorized]]` обновлена (impl). **ИТОГ — МОДУЛЬ СЕТЕВЫХ ПРИНТЕРОВ ПОЛНОСТЬЮ ГОТОВ:** обнаружение (спулер+ARP+static+скан) → опрос (SNMP/IPP/HTTP каскад, 8 вендор-драйверов) → хранение (история счётчиков/расходников, retention) → дашборд `/printers` (счётчики/IP/компы-источники/даты + сверка с print_jobs + принтеры на карте сети + кнопка опроса). Фоновый опрос (`printer_poll_enabled`) + активный скан (`active_scan`) ВКЛючены по умолчанию в `server/config.json`. **NEXT = строго user-directed** (живой прогон на реальной сети с принтерами — увидеть реальные данные; либо новая задача).
- **CURRENT (2026-06-19): ТИХИЙ АГЕНТ — невидимый сбор + развёрнутая 4ч-каденция — DONE, MERGED `935849c` (no-ff; feat `af12a32`, branch `fix/agent-silent-collection`), PUSHED.** Запрос юзера: работа агента не должна быть видна пользователям; старт при включении + каждые 14400с; без всплывающих/моргающих окон; смерджить и запушить. **Корень «морганий»:** `client/collectors/ps.py::run_ps` (единый раннер ВСЕХ коллекторов + трей-cert-проверки) спавнил `powershell` без подавления окна → чёрные консоли мелькали (особенно трей в СЕССИИ ЮЗЕРА — SYSTEM-агент в session 0 невидим, трей нет). Фиксы: (1) `ps.py` `creationflags=CREATE_NO_WINDOW` (Windows; `getattr(subprocess,"CREATE_NO_WINDOW",0)` off-Win — кросс-платформенно, POSIX `0`=no-op) — гасит ВСЕ powershell-окна в любой сессии, с `capture_output=True` подавление окна ⊥ перехвату stdout; (2) `packaging/srp.spec` агент `console=True→False` (фоновый процесс без своей консоли; tray уже windowed, setup остаётся console для UAC/инсталл-UX); (3) `client/agent.py` `setup_logging` не вешает console-`StreamHandler` при `sys.stderr is None` (windowed-build → stderr None, иначе raise на каждой записи); файловый хендлер несёт логи headless; (4) `client/deploy/config.template.json` ВСЕ пять `*_interval_sec`→14400 (развёрнутая каденция была heartbeat 300 / events+print 900 — реальный деплой «работал» каждые 5 мин; `config.py`-дефолты уже 14400, но ШАБЛОН ДЕПЛОЯ их перекрывал — вот почему правка 2026-06-18 не дала эффекта в поставке); (5) `server/db.py` порог дашборд-staleness `_STALE_AFTER_SEC` 900с→`_AGENT_CADENCE_SEC*2+900 ≈ 8.25ч` (иначе при 4ч-каденции ВЕСЬ флот «устарел» между биениями) — **ТОЛЬКО дашборд-флаг/KPI (get_devices/pipeline-metrics/dashboard.py), НЕ trust-гейтинг** (строка «stale» в `_GATE_FAIL_STATES` — отдельный per-source trust-вердикт из `server/trust`, не этот wall-clock-порог; проверено). Note: `get_pipeline_metrics` stale-SQL сравнивает `isoformat()` ('T') с `datetime('now')` (пробел) — пред-существующая format-хрупкость, НЕ трогал; тест бьёт по надёжному `age_seconds`-пути. (6) review-фикс `setup.py`: `--once` валидация теперь пишет диагностику в `validate.log` и читает хвост оттуда (windowed EXE без stderr → иначе install.log пустой при сбое; RMM/GPO-триаж сохранён). TDD: `test_ps_result` (windowless creationflags), `test_agent_logging` (stderr=None guard), `test_dashboard_api` (staleness ≥ одной каденции, < 2 каденций не stale). **Security-review (Opus, R4 agent/PS) = APPROVE-WITH-FIXES — все 3 применены** (MED setup.py-диагностика + LOW docstring/headless-note); инварианты PASS: `[[agent-stdlib-only]]` (getattr/subprocess/sys = stdlib), `[[agent-powershell-51-floor]]` (`creationflags`/`CREATE_NO_WINDOW` = OS-флаг subprocess, НЕ PS-cmdlet/param; argv не менялся), template policy-only без секретов, приватность/telemetry-surface не тронуты. Gate зелёный: ruff/mypy[69]/bandit clean, pytest cov **94.22%**, SMOKE OK. CHANGELOG: Changed (каденция) + Fixed (моргание). **PyInstaller-сборку НЕ перезапускал** (build вне `make check`; `console=False` — статическая правка spec, проверяется живой сборкой при следующем деплое). `client/config.json`/`org_directory.json` (чужие незакоммиченные diff) НЕ трогал/НЕ стейджил.
- **2026-06-18: ИНТЕРВАЛ ТЕЛЕМЕТРИИ АГЕНТА → 14400 сек (4 часа).** Все пять интервалов в `client/config.py` (`inventory_interval_sec`, `historical_interval_sec`, `heartbeat_interval_sec`, `events_interval_sec`, `print_interval_sec`) приведены к единому значению 14400. Агент шлёт всё при старте, затем каждые 4 ч. Только `client/config.py` — контракт/схема/сервер не тронуты.
- **CURRENT (2026-06-18): МОНИТОРИНГ СЕТЕВЫХ ПРИНТЕРОВ — ИСПОЛНЕНИЕ ПО ЭТАПАМ (автономно, inline; user-directed 2026-06-18 «экономь токены, динамический effort/модель, не спрашивай»).** Запрос: модуль мониторинга сетевых принтеров (SNMP/IPP/HTTP, расходники, счётчики, ошибки, обнаружение). Брейншторм → решения: (1) цель = телеметрия С САМИХ устройств — печать по ПК уже закрыта `print_jobs`/`/print`, не переписываем; (2) подход **A** — чистый stdlib SNMP-движок (BER на сокетах + IPP/HTTP `urllib`) в `server/printers/`, запуск на сервере, переносим в агент позже без переписывания (ответ на топологию «по-разному»); серверный сбор пишет в БД минуя контракт; (3) обнаружение тихое (порты спулера агента `Get-Printer`/`Get-PrinterPort` + ARP + список инженера), **активный скан ЗА стоп-гейтом безопасности** (выкл. по умолчанию, как сетевая Ф3); (4) телеметрия **информационная**, НЕ в trust/scoring, недоступный принтер → UNKNOWN; (5) **алертов в v1 НЕТ** (решение пользователя). Драйвер-реестр (вендор по `sysObjectID`) + единая база OID из 4 репозиториев (alfonsrv/printer-monitoring, ynlamy/check_snmp_printer, torgeirl/printer_tools, vvalchev/printer-scanner) + RFC 3805/2790. Spec `docs/superpowers/specs/2026-06-17-printer-monitoring-design.md` + план (7 этапов: 0 база OID · 1 SNMP-движок · 2 драйверы+классификатор · 3 обнаружение · 4 планировщик+БД · 5 вендоры+IPP/HTTP · 6 дашборд `/printers`+сверка · 7 скан за гейтом; Phase 0/1 расписаны по шагам с кодом, 2–7 task-level just-in-time) `docs/superpowers/plans/2026-06-17-printer-monitoring.md`. Закоммичены (merge `e96ddae`). **PHASE 0 (база OID) DONE + MERGED `368578a` (no-ff; feat `020ac28` + review-fix `d47cf8a`, branch `feat/printers-phase0-oids`), PUSH по команде.** `server/printers/oids.py` = `STANDARD` (скаляры MIB-II system + Printer-MIB General) + `TABLES` (расходники/лотки/ошибки/крышки/консоль/HOST-RESOURCES — базы для walk) + `vendor_for_sysobjectid` (enterprise-PEN→вендор, UNKNOWN для неизвестных; 12 PEN); OID = RFC 3805/2790/1213 канон. Тесты `tests/test_printer_oids.py` (presence/dedup-инвариант/vendor + non-enterprise→None). Гейт зелёный: ruff/mypy[59]/bandit clean, cov **94.18%** (oids.py 100%), SMOKE OK. **Code-review (Sonnet): CRITICAL+HIGH (якобы prtInput колонки .5/.7/.8/.9/.10) ОТКЛОНЁН — ложное срабатывание (галлюцинация таблицы RFC); авторитетно опровергнуто `oidref.com` (реестр OID): канон `prtInputMaxCapacity=.9`/`CurrentLevel=.10`/`Status=.11`/`MediaName=.12`/`Name=.13` — `input_*` и тест верны, не тронуты. LOW принят** (комментарий `supply_class` enum исправлен: supplyThatIsConsumed(3)/receptacleThatIsFilled(4)). Урок: скелет плана (Step 3) ронял dedup-тест дублем `prt_general_serial`=`prt_serial` → дроп алиаса (тесты=контракт). CHANGELOG НЕ трогал (фаза чисто внутренняя, ноль операторо-видимого поведения — строку добавлю на первом видимом этапе). 4-репозиторный скрейп OID НЕ делал: стандартные OID = RFC-канон, вендор-специфика идёт в Phase 5 по карте файлов (экономия токенов). **PHASE 1 (stdlib SNMP-движок) DONE + MERGED `d20e4bf` (no-ff; feat `7bf3ae7` + security-fix `0f2074c`), PUSH по команде.** `ber.py` (BER-кодек: длина/TLV/INTEGER/OCTET/NULL/OID, кап тела OID 128б) + `snmp.py` (v1/v2c на UDP, ТОЛЬКО ЧТЕНИЕ: build GET/GETNEXT/GETBULK, `parse_response`, `snmp_get`, `snmp_walk` GETNEXT с guard prefix/no-progress/max_rows; широкий except→{}, OSError-транспорт→{} не виснет/не краш, SET не реализован). **Security-review (Opus)=APPROVE-WITH-FIXES, все применены:** HIGH-1 (форджинг показаний)→резолв host→IP + приём только от целевого IP с совпадающим случайным (secrets) request_id; MEDIUM (CPU-ампл. OID)→кап 128б; LOW усечённая длина→ValueError; LOW широкий except + fuzz-тест; LOW-5 wall-clock walk отложен в Phase 4. 27 тестов (ber 10/snmp 14/oids 3), гейт зелёный: ruff/mypy[61]/bandit clean, cov **94.15%** (ber/oids 100%, snmp 90%), SMOKE OK. Отклонения (осознанные): walk на GETNEXT не GETBULK (таблицы малы); холодный re-review пропущен (SendMessage недоступен; фиксы = механич. применение одобренных реком., покрыты тестами). **PHASE 2 (драйверы+классификатор) DONE + MERGED `276ef87` (no-ff; feat `3f37b26` + review-fix `85b1ad9`), PUSH по команде.** `models.py` (frozen PrinterReading/Supply/Tray/PrinterError; сентинелы Printer-MIB <0→None; percent только при известных level&max>0; `Supply.class_` consumed/receptacle; `PrinterError.code` Optional) + `drivers/standard.py` (read(session,ip): скаляры .get / таблицы .walk по `oids.TABLES`) + `drivers/__init__.py` (get_driver(sysObjectID)→fallback standard; vendor-драйверы в Phase 5) + `classify.py` (is_printer: только .43 / hrDeviceType=printer / IPP; vendor-enterprise сам по себе НЕ принтер) + `collector.py` (probe→classify→driver→read; не принтер/нет ответа→None) + `snmp.SnmpSession`. **Code-review (Sonnet)=APPROVE-WITH-FIXES:** HIGH `_sorted_indices` guard (нечисловой/пустой индекс не роняет read через int()), MED `PrinterError.code` Optional + чтение `supply_class`, LOW DRY/коммент-статус; ОТКЛОНЁН context-manager SnmpSession (YAGNI, утечки нет). 48 printer-тестов, гейт зелёный cov **94.28%** (пакет printers 95%), SMOKE OK. **PHASE 3 (тихое обнаружение) DONE + MERGED `a9593a0` (no-ff; feat `9714638`, branch `feat/printers-phase3-discovery`) + PUSHED (origin/main `e0a44b3..a9593a0`, FF +15 коммитов — прежние phase 0/1/2 «push по команде» тоже ушли на origin).** Контракт additive: `PrinterPortHint{name≤256, ip≤64}` + `HistoricalPayload.printer_ports` (cap `PRINTER_PORTS_MAX=256`), БЕЗ bump CONTRACT_VERSION (0.1.0). Агент `client/collectors/printer_ports.py` (stdlib, WinPS 5.1 `Get-Printer`+`Get-PrinterPort`→host; **только RFC1918-литералы** — хостнеймы/публичные/IPv6-non-mapped дропаются, языконезависимо: парсим лишь IP, имя = непрозрачная метка; имя клипается до 256 чтобы один кривой принтер не ронял ВЕСЬ historical-конверт в 422; дедуп по ip, кап 256), влит в `historical.py` БЕЗ source_health (**информационно, НЕ trust-домен** — observability старых агентов НЕ штрафуется; spec §12). Серверное хранение автоматическое (store_historical кладёт весь payload JSON; pipeline/evaluate_trust НЕ трогали). `server/printers/discovery.py` = чистый `merge(agent_hints+arp_snapshots+static_ips)→[PrinterCandidate]`, дедуп serial>MAC>IP (serial появится на SNMP-пробе; пока MAC-collapse при смене IP), RFC1918 re-filter (defense-in-depth), детерминированный сорт по ip; `is_rfc1918` переиспользован в `config.py`. `server/printers/config.py` = `PrinterConfig`+`load_printer_config` (interval≥60, community=public, version∈{0,1}=v2c-дефолт, static_ips RFC1918-фильтр, **active_scan=False, включается ТОЛЬКО явным `is True`** — стоп-гейт активного скана). `db.get_printer_port_hints()` (latest-by-id, статичный параметризованный SQL, read-cap 256). 28 printer-тестов (contract 7 / collector 9 / discovery+config+db 12; TDD RED→GREEN). Гейт зелёный: ruff/mypy[69]/bandit clean, cov **94.22%** (config 100% / discovery 90% / schema 100%), SMOKE OK. **Security-review (Opus, R4) = APPROVE-WITH-FIXES:** все 9 инвариантов + PS-инъекция PASS, 0 Crit/High; применён L3 (агент клипает имя — анти self-422); M1/L1 = локальные `client/config.json`/`org_directory.json` НЕ коммитил (§6: staged ровно 10 файлов фичи). CHANGELOG: строка в Added (агент теперь шлёт IP принтеров — приватностно-смежно). **Discovery = библиотека+контракт, НИ во что не вшита (route/scheduler/ingest)** — правильно держит Phase 3 НЕ-сканирующей. **NEXT = Phase 4 (планировщик + таблицы БД): `server/printers/scheduler.py` (lifespan-loop + ThreadPoolExecutor опрашивает кандидатов из `discovery.merge`, таймаут/ретрай на хост, guard ВНУТРИ как device-ghost-cleanup); `db` таблицы `printers` (latest inventory) / `printer_readings` (append-only скаляры + JSON-blob деталь + retention-cap + миграции additive-идемпотентны); `server/main.py` lifespan (рядом с retention sweep, `contextlib.suppress(CancelledError)`); `server/config.py` wiring `PrinterConfig`. ⚠ Phase-4 вызыватель ОБЯЗАН пере-проверять `config.active_scan` перед любой пробой (ревью-нота); ревью `security-reviewer` Opus — SQL/loop/ресурсы.** Применимая память: `[[agent-stdlib-only]]`/`[[agent-powershell-51-floor]]`/`[[language-independence]]`.
- **CURRENT (2026-06-16): УБОРКА ПРИЗРАКОВ-УСТРОЙСТВ — РЕАЛИЗОВАНО, GATE ЗЕЛЁНЫЙ, security-review идёт; branch `fix/device-ghost-cleanup` (server-only; агент/контракт/CONTRACT_VERSION НЕ тронуты).** Причина: фикс `14c6a97` сделал device_id случайным per-install → переустановка/сброс/clone-reset = новый id → старая запись осиротела и ВЕЧНО висит во флоте; `get_devices` отдавал всё без фильтра свежести; функции удаления НЕ было (Explore: 11 таблиц по device_id). Решения юзера (4+2): история при переустановке НЕ нужна (НИКАКИХ железных примет — агент чист), клоны=физ.ПК из образа, призраков УДАЛЯТЬ совсем, историю НЕ склеивать, удаление БЕЗ auth (доверенная LAN, как существующие ack/meta) но POST+подтверждение, авто-удаление при молчании >30 дней (`device_retention_days`). Сделано: (1) `db._DEVICE_TABLES` (11 таблиц = single-source) + `delete_device()` (ОДНА транзакция, чистит все) + `purge_devices_silent_for(days, *, dry_run)` (silence по СЕРВЕРНОМУ `last_seen` через `_age_seconds`; непарсимый last_seen НЕ трогаем; days<0→ValueError); схема-интроспекшн тест валит забытую таблицу; (2) `config`: `device_retention_days=30`/`purge_interval_hours=24`; (3) `main.py` lifespan: sweep при старте + asyncio-loop, чистая отмена через `contextlib.suppress(CancelledError)`; (4) api: POST `/devices/{id}/delete` (404 если нет) + POST `/devices/purge` (PurgeBody days ge=1 le=3650 → days=0 не сотрёт флот; dry_run=preview); (5) дашборд: кнопка ✕ у строки (delegated, encodeURIComponent, confirm) + тулбар «Очистить офлайн…» (preview→confirm→purge) + CSS `.delbtn/.toolbtn`. **Грабли §5 подтвердились**: PostToolUse-ruff срезал `asyncio/contextlib/logging` сразу после imports-only edit (usage был в ПОЗЖЕ добавленных функциях) → вернул когда usage уже на месте. 17 тестов (test_device_cleanup.py 10 + test_device_cleanup_api.py 7). **GATE ЗЕЛЁНЫЙ**: ruff/format/mypy[57]/bandit clean, ПОЛНЫЙ pytest exit 0, cov **94%**, SMOKE OK. Спека `docs/superpowers/specs/2026-06-16-device-ghost-cleanup.md`; CHANGELOG (Added). **Security-review (Opus, SQL/ingest) = APPROVE-WITH-FIXES**: items 1–4 (SQL-инъекция/полнота `_DEVICE_TABLES`/destructive-safety/XSS) + отмена loop = CLEAN; 1 HIGH — startup-sweep был без try/except (транзиентная ошибка БД, напр. locked, роняла бы старт сервера) → ФИКС: guard перенесён ВНУТРЬ `_run_retention_sweep` (защищает и старт, и loop; loop упрощён), гейт перепроверен зелёным (ruff/mypy[57]/17 тестов/SMOKE). → commit (touched-only, НЕ client/config.json) + merge --no-ff в main; push ТОЛЬКО по команде.
- **CURRENT (2026-06-15): ФИКС колонки «Сертификат» во флоте («живой мониторинг») — DONE, MERGED `6d2e566` (no-ff; fix `98fd5c3`, branch `fix/fleet-cert-personal`), PUSHED.** Симптом (юзер): в таблице флота графа «Сертификат» = прочерк, хотя в карточке машины виден личный серт и срок. Причина: `server/db.py::_cert_summary` агрегировал ТОЛЬКО `certificates` (машинные серты, видимые SYSTEM-агенту из LocalMachine — на этой машине пусты), игнорируя `user_certificates` (личные серты из трей-спула, этап 8 — они и в карточке). Комментарий шаблона `_fleet_body.html` врал («personal cert»). Фикс: `_cert_summary` складывает оба списка и берёт ближайший срок (раннее предупреждение — не прячем более близкий риск); комментарий поправлен. Read-side only, только метаданные (без ключей). TDD: 2 новых теста в `test_dashboard_api.py` (личный-только серт → срок виден; ближайший из машинного+личного). Security-review (Opus, серт-правило §3) = **APPROVE** (0 findings: ключей нет, оба списка pydantic-validated + cap 64, в шаблон уходит только int, autoescape ON). Гейт зелёный: ruff/mypy[57]/bandit clean, cov **94.31%**, SMOKE OK. CHANGELOG: строка в Fixed.
- **CURRENT (2026-06-15): «Печать сегодня» во флоте вместо 30 дней — DONE, MERGED `ae22a68` (no-ff; feat `b70290e`, branch `fix/fleet-print-today`), PUSHED.** Запрос юзера: на странице «живой мониторинг» сменить «Печать (30 дней)» на «Печать сегодня» (счёт только за текущий день). `server/db.py::get_fleet_print` получил kw-параметр `today`: при `today=True` фильтрует задания по границе начала МЕСТНОГО дня, выраженной в UTC (`_local_day_start_utc`) и переданной bind-параметром `?` (лексическое сравнение со штампами агента `...ToUniversalTime().ToString('o')` — не зависит от парсинга 'Z' в SQLite, держит индекс); `/api/v1/fleet/print` получил флаг `today`; обе выборки печати на странице флота (построчная колонка + обзор) ходят с `?today=1`; подпись/тултип на «сегодня». `/print` (полная аналитика) остался 30 дней. TDD: 2 теста в `test_print_tracking.py` (сегодня считает только сегодняшние; 30-дневное окно — оба). Security-review (Opus, SQL-правило §3) = **APPROVE** (0 findings: today-путь параметризован, days-путь = пред-существующий nosec с `_clamp_days` 0..365, FastAPI типизирует query → 422 на мусор, окно только сужает данные). Гейт зелёный: ruff/mypy[57]/bandit clean, cov **94.28%**, SMOKE OK. CHANGELOG: строка в Changed.
- **CURRENT (2026-06-13): ТРЕЙ ЭТАП 3 (справочник орг/отделов) + XSS-ФИКС — DONE, MERGED `33a18e0` (no-ff), PUSHED (origin/main `33a18e0`, чистый fast-forward `1ba04fe..33a18e0`, расхождения origin НЕ было).** Слайсы: **A** `1405051` (`server/org_directory.py`+`org_directory.json`: mtime-reload, битый JSON→лог+старая копия, неизвестный код→чип); **B+C** `bb2c52e` (`get_print_analytics` отдаёт сырые коды; `fleet_print_analytics` декодирует render-time, флаг `known`→чип; CSV `org_name`/`dept_name`; PATCH-meta `comment`; free-text `department` DEPRECATED→`dept_code`+COALESCE); **D** `3151772` (SSR-декод во флоте+устройстве с чипом «нет в справочнике», edit-поле device.html→comment; бонус-наблюдаемость: версия агента у машины, «устар.»=ниже max-версии флота, «NEW ≤7д» по first_seen + KPI-фильтры). **Security-review (Opus) нашёл CRITICAL stored-XSS:** этап 3 переключил подпись отдела на графике `/print` со свободного текста оператора на **агент-управляемый `dept_code`** → течёт в Plotly tick + `hovertemplate %{x}` (Plotly рендерит pseudo-HTML `<b>/<a>/<span>`, выполняется как innerHTML; данные через `fetch()` JSON → autoescape Jinja не достаёт). **ФИКС `b5cc412` = output-encoding в КАЖДОМ JS-приёмнике:** один общий `window.srpEsc` в `base.html` (единый источник истины), применён в `print.html` (отдел=C1, принтер, пользователь, таблица принтеров, error-путь) + `fleet.html` (имя ПК + принтер = давний C2). Plotly round-trip entities → легитимные Cyrillic/`&`-имена без изменений; `srpEsc` = ТОЛЬКО HTML-контекст (задокументировано — не годится для JS-string/URL/unquoted-attr); substring-ДО-escape безопасен. +3 регресс-пина (`test_org_directory.py`: хелпер едет + каждый приёмник через srpEsc). **Re-review (Opus) = APPROVE** (эмпирически Node: экранирование рушит Plotly pseudo-HTML, пробой `title="…"` не проходит, `hasReal` сравнивает сырой `r.dept`, `|safe`/autoescape не трогали, пропущенных приёмников нет; device/pipeline/netmap/_fleet_body = SSR/textContent — безопасны). CHANGELOG: Security-строка. Gate green: ruff/mypy[46]/bandit clean, cov **94.29%**, SMOKE OK (`/`,`/device`,`/print` рендерятся 200). Память `[[dashboard-xss-srpesc]]`. **ЭТАП 4 (трей core) — DONE, MERGED `c5147c3` (no-ff), PUSHED (origin/main `c5147c3`, расхождения origin НЕ было).** `client/tray/` = ctypes Shell_NotifyIcon (+TaskbarCreated) + hidden window + меню, tkinter-панель/пароль-промпт ДОЧЕРНИМИ процессами (краш tkinter не роняет иконку), single-instance мьютекс, `state.py` = чистая логика (icon_state worst-of, freshness 15м, support-клипборд, пароль-гейт 3×/5мин персистентный в tray_state.json). 14 файлов staged (3 .ico бинарно); `client/config.json` НЕ коммитнут. Security-review (Opus) APPROVE-WITH-FIXES → оба фикса применены+перепроверены; живой ctypes+message-loop+icon-fallback smoke на Windows. Stage 4 пробросил `cert_level="unknown"` в иконку — этап 5 ставит реальную проверку. **ЭТАП 5 (cert-движок v2, R4) — DONE, MERGED `74735db` (no-ff; feat `7bbc36a`), PUSH далее:** `client/tray/certs.py` (чистый движок: epoch-PS `Cert:\CurrentUser\My ? HasPrivateKey` → `ToUnixTimeSeconds` [языконезависимость, НЕ локализованные даты]; группировка по нормализ. Subject CN — валидный преемник гасит старый до/после истечения + однократный инфо-балун; `should_nag`: >14д тишина / 8–14д warn-балун каждые 4ч / ≤7д alert каждые 4ч / истёк ≤7д 1×календарный-день / истёк >7д тишина+красная остаётся; `tray_require_cert` пусто→1/день) + config additive `tray_cert_warn_days=14`/`tray_notify_hours=4`/`tray_require_cert=False` (`helpdesk_contact` уже есть); анти-спам в tray_state.json (`_certs` ключ, не трогает `_gate`); приватность §5 — закрытые ключи НИКОГДА не читаются (только булево HasPrivateKey); PS-сбой→UNKNOWN (не ложное «истёк»); пустое хранилище `[]` ≠ сбой `None`. Трей (сессия юзера) проверяет `Cert:\CurrentUser\My`, невидимый агенту-SYSTEM; 30-мин ререндер в icon-цикле + read-only строка серта в панели. Gate green: ruff/mypy[53]/bandit clean, cov **94.29%**, SMOKE OK. **Security-review (Opus) = APPROVE-WITH-FIXES** (0 Crit/High/Med; ключ структурно недостижим; атакующий Subject CN НЕ доходит ни до одного render/format/exec-приёмника — только in-memory ключ группировки + нерендеримый `CertInfo.subject`) → **3 LOW применены**: `Select-Object -First 200` кап stdout, prune `_certs` до текущих отпечатков (нет роста), доком race у `save_cert_state`. 22 теста `test_tray_certs.py` + 3 `test_tray_state.py`. Память `[[cert-subject-grouping]]`. **ЭТАП 6 (build+setup одной командой, R4) — DONE, MERGED `a90b21b` (no-ff; feat `3a5a77d`; origin был синхронен до мержа — «PUSH далее» этапа 5 устарело, `74735db` уже на origin).** Прошлая сессия написала почти всё дерево этапа 6; эта проверила критически + доработала. Собрано: `build.bat`+`packaging/srp.spec` (PyInstaller onedir agent+tray, onefile uac-admin setup; build-only dep, `client/` остаётся чистым stdlib) → 3 артефакта в `dist\share\`; `client/deploy/setup.py` = установщик одной командой (чистая тестируемая логика сверху: parse/auto-quiet, validate `^[A-Za-z0-9_-]{1,16}$`, merge config, argv robocopy·icacls·schtasks·reg·wevtutil как ДАННЫЕ; тонкий Windows-shell снизу): robocopy payload→C:\SRP → **icacls /inheritance:r** (корень C:\ даёт Users ЗАПИСЬ → закрываем; это и есть защита config.json, не пароль) → merge(template<prior<params: пароль→PBKDF2, `device_id` сохраняется, UTF-8 без BOM) → wevtutil best-effort → `--once` валидация → SYSTEM-задача из `task_template.xml` (S-1-5-18, без PowerShell) + HKLM Run трея → старт; exit 0/2/3/4/5 для RMM/GPO, `C:\SRP\install.log`, `--uninstall/--purge/--no-tray/--allow-offline`; старый `installer/setup.py` удалён. 39 тестов `test_setup_logic.py`. **Security-review (Opus, R4) = no Critical/High.** Правки ревью применены: (1) **`_config_loadable` был МЁРТВЫМ кодом** → подключён как гейт (конфиг без server_url → SYSTEM-автозапуск НЕ регистрируется; касается и offline); (2) откат задачи при сбое Run-ключа (нет осиротевшего автозапуска); (3) `--once` вывод захватывается + редактированный хвост в install.log (агент уже редактирует URL, секрет не печатает); (4) `template_has_server` подключён в validate (`--server` из шаблона — было мёртвым). **H1 (пароль/токен в argv setup.exe → виден в process list / RMM-аудите) = осознанный tradeoff спеки §6** (митигация: скрытая шара под ACL «только ИТ»; на аудируемых хостах — без флагов, секреты в config.json вручную; `--password-stdin` спекой отвергнут). M4: `.gitignore` сужен с `*.log` до конкретных имён; рантайм `status.json`/`print_state.json`/`agent.log` добавлены в ignore. Gate: ruff/mypy[55]/bandit clean, cov **94.29%**, SMOKE OK. Память `[[installer-acl-deployment]]`. **Реальную сборку PyInstaller в этой сессии НЕ запускал** (ручной шаг dev-машины, в гейт не входит; риск к проверке живьём: два onedir-приложения кладутся в ОБЩИЙ `payload\` — при одинаковом тулчейне DLL идентичны, но подтвердить сборкой). **Оставлено untracked НАМЕРЕННО:** dev-лаунчеры `start_agent.bat`/`start_server.bat`/`agent_ip.bat` (не в плане этапа 6 — спросить, нужны ли в репо), заметки `*.txt`/`.bak`, `screenshots/`, локальный `client/config.json`. **ЭТАП 7 (генератор BAT в дашборде, R2) — DONE, MERGED `52d5967` (no-ff; feat `9870ebe`), PUSH далее.** Страница `/deploy`: выбор орг/отдела из справочника → готовый `setup.exe`-BAT с кодами + server_url (дефолт из хоста запроса), кнопка «Скопировать»; пароль/токен = плейсхолдеры `<ПАРОЛЬ>`/`<ТОКЕН>` (дашборд без auth секреты НЕ хранит); read-only (ничего не пишет). `OrgDirectory.as_picker()` (code→name, JSON-ready, сорт по коду) + nav-ссылка. **XSS-safe by-construction**: SSR `<select>` через autoescape, JSON-остров `|tojson` (экранирует `</script>`), отделы строятся `new Option()` (textContent), BAT в `textarea.value` — без innerHTML/Plotly-приёмников. Code-review (Sonnet) = WARNING (XSS чистый); правки: кавычки на динамич. аргументы BAT (код с пробелом = один арг → чистый отказ setup-валидации), copy Clipboard-API-first (флаг «скопировано» только при успехе), детерминированный mtime-reload в тестах. 8 тестов (test_deploy_page.py 6 + as_picker 2). Память не добавлял (XSS-подход уже в `[[dashboard-xss-srpesc]]`). Gate: ruff/mypy[55]/bandit clean, cov **94.31%**, SMOKE OK. **ЭТАП 8 (личные user-серты во флоте, R4) — DONE, MERGED `ec55164` (no-ff; feat `cc74353`), PUSH далее.** Архитектура: SYSTEM-агент не видит `Cert:\CurrentUser\My`, поэтому трей (сессия юзера) пишет спул `C:\SRP\spool\usercerts-<user>.json` (метаданные серта + имя пользователя, ключи НИКОГДА); `client/collectors/user_certs.py` строго валидирует недоверенный user-writable ввод (капы файлов/размера 64К/числа, типы, дедуп по (owner,thumbprint), окно свежести 30д, лимит 64, epoch→ISO) → в historical payload (НЕ в trust/scoring — информационно, хранится JSON-блобом, отдельной колонки нет); contract `CertInfo.owner`+`HistoricalPayload.user_certificates` (additive, cap 64, generous max_length бэкстоп; без bump CONTRACT_VERSION); `setup.py` создаёт `spool`-подкаталог + icacls ADD `*S-1-5-11:(OI)(CI)M` ТОЛЬКО на нём (остальное C:\SRP=Users:RX), best-effort/non-fatal; `device.html` блок «Личные сертификаты пользователей» (владелец+срок, autoescape XSS-safe). Юзер выбрал «с именем пользователя». **Security-review (Opus, R4) = no Crit/High, 6/6 инвариантов pass** (hostile-input safe: stat-before-read, no traversal, no billion-laughs; no key material; ACL scoped; XSS autoescaped; trust-isolated; contract additive). MEDIUM применён (`max_length` на строки `CertInfo` — backstop прямого token-poster без отклонения реальных DN). LOW (forgeable `written_at`) = benign-by-design, прокомментирован. 28 тестов. Gate: ruff/mypy[57]/bandit clean, cov **94.31%**, SMOKE OK. Память `[[usercert-spool-tray-to-agent]]`. **ТРЕЙ-ПЛАН (этапы 1–8) ПОЛНОСТЬЮ ЗАКРЫТ.** Ранее этой сессией (`ff9a1b9`): чистка .bat — удалены устаревший `install.bat` (заменён stage-6 setup.exe) + дубль `run_agent_persistent.bat`; добавлены лаунчеры `start_agent`/`start_server`/`agent_ip` (start_agent улучшен: arg сервера + config-error backoff); `docs/agent-install.md` (сборка+установка, все ключи, без воды). **BUILD ПРОВЕРЕН ЖИВЬЁМ (merge `b804e8f`, fix `203ae10`):** `build.bat` падал — spec-пути относительны `packaging/`, не корня (PyInstaller resolve `srp_agent_main.py not found`); заякорены на `ROOT=parent(SPECPATH)` (3 entry-скрипта + иконки). Теперь exit 0: `dist\share\` = `setup.exe` (5.5М, onefile UAC) + `config.template.json` + `VERSION` + `payload\{srp-agent.exe 1.6М, srp-tray.exe 1.7М, task_template.xml, _internal\...\srp_ok.ico}` — оба onedir в ОБЩЕМ payload с одним `_internal` (риск «слияния onedir» из ledger снят живой сборкой). PyInstaller — build-only (6.6.0), `client/` остаётся stdlib. **УСТАНОВКА ПРОВЕРЕНА ЖИВЬЁМ НА ЭТОЙ МАШИНЕ (RU Windows, server `192.168.1.99:8000`, merge `7f7f4bf`):** `setup.exe` exit 0; задача `\SRP Agent` (SYSTEM/HighestAvailable, BootTrigger) создана; агент работает как SYSTEM (`FPLUS$`), шлёт inventory/historical/heartbeat/events/print_jobs на сервер (выдержал транзиентный timeout, переотправил); `config.json` = 192.168.1.99 / org=1 / device_id resolved; HKLM Run→трей (стартует при логоне). **Две R4-багфикса локализованной Windows, найдены этой установкой:** (1) `icacls` английские `Administrators`/`Users` НЕ резолвятся на RU-Windows → закрытие прав падало (exit 4) → язык-независимые SID `S-1-5-18`/`S-1-5-32-544`/`S-1-5-32-545`; (2) `schtasks /create /xml` отвергает UTF-8 («failed to switch encoding», (1,40)) → задача не создавалась (exit 5) → setup перекодирует task XML в UTF-16 (BOM). Оба + spec-path fix (`b804e8f`) = установщик/сборка теперь работают на RU-Windows. Память `[[installer-localized-windows]]`. **NEXT = строго user-directed.** СТОП-ГЕЙТ без изменений (сеть Ф3 = письменное разрешение безопасности; §7 L1/L2/L3 = prod-данные).
- **CURRENT (2026-06-12, вечер): ТРЕЙ-КЛИЕНТ — DESIGN SPEC **v2** (после фидбэка пользователя), ЖДЁТ УТВЕРЖДЕНИЯ.** `docs/superpowers/specs/2026-06-12-tray-client-design.md` (v1 commit `543b9b4`; R4). **v2-дельта:** (а) установка = ОДНА команда/BAT со ВСЕМИ параметрами (--server/--org/--dept/--password/--token/--helpdesk/--comment/--no-tray/--allow-offline; авто-quiet при заданных обязательных; интерактив = fallback без параметров; exit codes 0/2/3/4/5 + C:\SRP\install.log для RMM/GPO; секреты в BAT → скрытая шара `srp$` под ACL «только ИТ»; граница: параметры = что отличается между машинами, config.template.json = политика орг.); (б) НОВЫЙ §7 серверный справочник `org_directory.json` (файл настроек, НЕ админ-UI — дашборд без auth; mtime-reload; битый JSON → лог + старая копия) → **render-time decode** названий орг/отделов в fleet/device//print/CSV (имена НЕ пишутся в БД → переименование мгновенно на всю историю), чип «нет в справочнике» (ловит опечатки BAT, телеметрия не отклоняется), **УНИФИКАЦИЯ один-источник-истины**: free-text `devices.department` (по ней сейчас группирует /print + PATCH meta) DEPRECATED → `dept_code`+справочник (COALESCE на переход), edit-поле device.html → comment; бонус-флот: колонка версий агентов + фильтр «устаревшие» + «новых за 7 дн» (agent_version/first_seen уже в devices — проверено grep); (в) **cert-движок v2**: после истечения 1 балун/КАЛЕНДАРНЫЙ день × 7 дн → потом тишина (красная иконка/панель остаются); **группировка по Subject CN** — валидный преемник гасит напоминания старого (продление ≠ шум) + однократный инфо-балун «обнаружен новый сертификат до …»; разные субъекты независимы; `tray_require_cert` опция; (г) этапы 1–8 (3=справочник, 7=опц. генератор BAT-команды в дашборде с плейсхолдерами `<ПАРОЛЬ>/<ТОКЕН>` — без секретов в открытом дашборде). Решения: (1) **две плоскости** — SYSTEM-агент (как есть) + `srp-tray.exe` в сессии каждого пользователя (HKLM Run), IPC = односторонний `status.json` (атомарный, без секретов), канала команд НЕТ; (2) **личный серт виден только из сессии пользователя** (SYSTEM видит свой CurrentUser — потому W1.2 его не покрывает) → проверка в трее: PS `Cert:\CurrentUser\My` epoch-даты, балун-напоминание каждые 4 ч в окне ≤14 дн (красный ≤7; 14 — правка пользователя при утверждении), анти-спам в `%LOCALAPPDATA%`, движок = чистая `should_nag`; (3) трей: ctypes Shell_NotifyIcon (+TaskbarCreated) + tkinter-панель ДОЧЕРНИМ процессом, пароль = существующий `config_password_hash` (выход/настройки; честно задокументировано «не защита от админа»); панель: орг/отдел, IP (RFC1918, live socket), серт, связь, печать сегодня/месяц, диск, аптайм, версия, helpdesk_contact, кнопка «Скопировать для поддержки»; (4) **печать fallback**: авторежим каждый sweep — журнал 307 включён? events : counter (CIM `Win32_PerfFormattedData_Spooler_PrintQueue` TotalPagesPrinted, дельты+reset-detect, `source` additive в контракт/БД, самовосстановление на events без двойного счёта); (5) **установка**: PyInstaller onedir (build-dep, не runtime — видение из «деплой агента.txt»), шара `\\server\srp$\` = setup.exe+payload+config.template.json+VERSION; setup спрашивает ТОЛЬКО код организации (dept опц.), robocopy, **icacls C:\SRP** (дефолт корня C:\ даёт users запись!), schtasks XML (без PS), Run key, wevtutil, валидация `--once`, `--quiet`/`--uninstall`; (6) этапы 1–6 (status.json → fallback → tray core → cert nag → build+setup → опц. user-cert fleet-спул). org/dept УЖЕ в контракте+transport (проверено grep). **ПЛАН ПРИНЯТ ПОЛЬЗОВАТЕЛЕМ (одна правка: окно предупреждения серта = 14 дн, было 30) → ИСПОЛНЕНИЕ ПО ЭТАПАМ.** План: `docs/superpowers/plans/2026-06-12-tray-client.md`; память: `tray-split-plane`/`cert-subject-grouping`/`print-counter-fallback`/`org-directory-render-time`. **ЭТАП 1 DONE + MERGED `f3882d9`** (status.json атомарный без секретов — пин-тест всего файла; явный RFC1918-allowlist вместо is_private (дрейф в 3.11+); счётчики печати по дням в print_state, 62 дн; transport last_ok_ts/last_error/buffer_depth; agent публикует в run_once + каждой итерации; code-review (Sonnet) APPROVE-WITH-FIXES → оба MEDIUM исправлены `92b6c0d`; gate: ruff/mypy/bandit clean, cov 94.28%, SMOKE OK). **Origin примирён merge'ом**: хэши `7b97305`+ СУЩЕСТВОВАЛИ на origin (см. Done) — урок «проверяй git log origin/main». PUSH разрешён пользователем («смерджи запушь в main») — пушим каждый этап. **ЭТАП 2 DONE + MERGED `0b15c76` → PUSHED (main `45082e6`)** (print fallback, §5 спеки): авторежим в КАЖДОМ sweep — `_detect_mode()` через `Get-WinEvent -ListLog …PrintService/Operational` → bool `IsEnabled` (локале-безопасно); журнал ON → events (Event 307, как было); OFF/нет → counter по CIM `Win32_PerfFormattedData_Spooler_PrintQueue.TotalPagesPrinted` (инвариант PerfFormattedData, не Get-Counter) — per-queue дельты, reset-detect (`cur<base → delta=cur`), `_Total`+виртуальные отсекаются в PS И в Python (defense-in-depth); **двойной счёт убит**: `reseed` на events→counter сбрасывает устаревшие baselines, first-sight сеет молча (lifetime-счётчик не эмитится как «напечатано сейчас»), counter→events safe через `last_sweep_ts`; `PrintJobRecord.source` Optional `events|counter` additive (`max_length=16`) — старый агент шлёт None, БЕЗ bump CONTRACT_VERSION; db: additive-колонка `source` + migration + backfill `'events'`, store+CSV-экспорт; counter-строки `job_id=None/user_name=None` (нет приватности юзера в fallback), partial-UNIQUE не дедупит NULL job_id; Evernote добавлен в фильтр виртуальных (живая машина); print_state.json v2 `{mode,baselines}` с graceful-degrade от v1 (`.get()`-гварды). **ЖИВАЯ CIM-проверка на ЭТОЙ машине** (журнал OFF → counter-путь реально активен; `Name`/`TotalPagesPrinted` подтверждены, Cyrillic-имена «Canon … (копия 1)» обрабатываются). 16 тестов `test_print_fallback.py`. Gate: ruff/mypy/bandit clean, cov **94.28%**, SMOKE OK; **security-review (Opus) = APPROVE** (0 Crit/High/Med; 1 LOW = pre-existing CSV formula-injection в printer/user_name, out-of-scope, `source` не усугубляет). Память `[[print-counter-fallback]]`. **Origin СНОВА разошёлся** — origin/main имел `68b8b3d Update CLAUDE.md` (R2 routing Sonnet·medium → Opus·low); примирён merge'ом `45082e6` (непересекающиеся файлы, конфликта нет) → урок «git log origin/main перед push» подтверждён в 3-й раз. NEXT: **этап 3 (справочник организаций R3)** — `server/org_directory.py`+`org_directory.json`, mtime-reload, decode helper render-time, fleet/device/print/CSV с названиями, чип «нет в справочнике», deprecate free-text `devices.department`→`dept_code`+COALESCE, бонус-флот: версии агентов + «новых за 7 дн». **СТОП-ГЕЙТ без изменений:** (1) **сеть Ф3 (активный скан)** требует ПИСЬМЕННОГО разрешения безопасности («выглядит как атака для EDR», спека §8 + tools-design §3, спека `2026-06-11-network-phase3-active-scan.md` уже лежит untracked); (2) **§7 L1/L2/L3** (label-loop/anomaly/survival) требуют production-данных (подтверждённые отказы, shadow-калибровка) — их нет. NEXT = строго user-directed (push? Ф3 с явным разрешением? заметки `суперсервер.txt`/`деплой агента.txt` как новые спеки?).

- **CURRENT (2026-06-10): NETWORK Ф1 + RUSSIAN ANALYTICS + BAT-FIX — ALL MERGED TO `main` + PUSHED (user: «закончи, смерджи и запушь все»).** Three no-ff merges: (1) **`dd85bce`** = network Ф1 (7 commits incl. review-hardening `66a8226`; dual subagent review NO Critical; RFC1918-strict `_is_internal`, link-speed sentinels→None, quality-counts-as-present, ping cap 4 literal-IP; accepted gaps documented in plan; **WinPS 5.1 has NO `Test-Connection -TimeoutSeconds`** — memory `[[agent-powershell-51-floor]]`). (2) **`5259982`** = `feat/analytics-russian-strings` `8b8e934`: finished the stashed WIP translation — ALL operator prose (factors/reasons/missing_evidence) of 6 engines (storage/battery/disk_fill/os_degradation/fleet_anomaly/trends) на русском; Latin terms kept (RSI/BSOD/SMART/KP41/Power-on/Windows Update); machine values untouched (lineage keys, direction/band/confidence enums, trust states); os_degradation factor labels were still English in the stash — translated; 12 tests re-pinned to Russian (dashboard untrusted-banner test now asserts the real banner «Идентичность недостоверна», not an incidental engine-reason substring); CHANGELOG line. (3) **`63e25c5`** = `fix/agent-bat-exit-code-2` `77149c6`: run_agent_persistent.bat routes exit 2 (validate_runtime_config SystemExit(2)) to the 30s config-error path (was only 1; offline_mode hint verified real — client/config.py:65). Gate green at every merge: ruff/mypy/bandit clean, **cov 94.14%**, **SMOKE OK**. `main` @ `63e25c5` (+CONTINUITY commit after) **pushed to origin**. **(4) CLAUDE.md rewritten (user-directed, 2026-06-10)** — merge of `chore/claude-md-cost-routing` `681da82`: dynamic cost-first routing R1 Haiku/low (mechanical/gate-fix/doc-line) → R2 Sonnet/medium (approved-plan task execution · tests · repro-bugfix · routine diff review) → R3 Opus/high (template-following new collector/engine · multi-file) → R4 Opus/max (contract/trust/scoring/PS/privacy/design — NEVER de-escalates); design@R4 → execute@R2; escalate on unsure/>3 files/2 fails; reviews split security-reviewer=Opus vs code-reviewer=Sonnet; new §5 lines (WinPS 5.1 floor, RFC1918-only, RU prose/EN machine values); §0 targeted-tests-while-iterating; §6 no-make-on-box note. Ф2 (карта сети + `network_health` axis + subnet anomaly; then add `network` to DOMAIN_SOURCES) и Ф3 (активный скан за гейтом) — следующие фазы, ждут команды. NEXT = user-directed (Ф2, или «суперсервер»/«деплой агента» заметки как новые спеки?).
- **SIDE STATE (2026-06-10): рабочее дерево.** `client/config.json` = ЛОКАЛЬНАЯ правка юзера для живого теста (localhost:8000 + реальный device_id) — **никогда не коммитить** (шаблон в репо пустой — W1 решение). Untracked заметки юзера: `docs/superpowers/specs/суперсервер.txt`, `деплой агента.txt`, `screenshots/`, `agent.log`, `client/print_state.json` — не трогать; «суперсервер»/«деплой» возможно будущие спеки, спросить при случае. Stash пуст (русификация завершена и смержена).
- **§7 Fleet Redesign — DONE, merged to main @ `dff4e51` (2026-06-09):** compact single-row fleet table (15px fonts), «Сертификат» столбец (cert-alert <17д; plain yellow иначе, без color-тиров), «Печать» столбец (async refreshPrint() → /fleet/print?days=30), device.html cert-section only active certs (days≥0). Tests: 529 passed, gates green.
- **Print Tracking + Analytics — DONE, merged to main @ `8d6d4e4` (2026-06-09). Фазы 1–15 завершены:** `print_jobs` collector (Event 307, incremental sweep, virtual filter), DB table + UNIQUE INDEX, `department` колонка на devices, `print_jobs` msg_type в pipeline (touch_device + store; no rescore), API (`/devices/{id}/print`, `/fleet/print`, `/fleet/print/analytics`, `/fleet/print/export.csv`, `PATCH /devices/{id}/meta`), `/print` страница с 5 Plotly.js-графиками + CSV-кнопка, nav-ссылка «печать» в base.html, print teaser на fleet/device страницах. 21 новый тест; 521 total, cov 94%, все gates green.** Ветка `feat/print-tracking` смержена и `main` запушен. Следующая задача — user-directed.
- **CURRENT (2026-06-09): P1 transport hardening DONE — (1) reconnect-jitter (random.uniform 0–2s в retry backoff); (2) idempotency_key (UUID4.hex) в конверте → server dedup (`check_idempotency`) → `duplicate:true` без повторной записи; (3) per-device rate-limit 30/60s → 429 (`check_rate_limit`); (4) `_IngestBodySizeMiddleware` Content-Length > 512KB → 413; (5) `server/ingest_guards.py` с `reset_guards()` + autouse-фикстура в conftest.py. Gate: 500 tests, cov 93%, ruff/mypy/bandit clean, smoke OK. §7 гейты (L1/L2/L3) не пройдены — требуют production-данных (confirmed failures, shadow calibration). NEXT = ожидание результатов code-review или user-направление.**
- **CURRENT (2026-06-09): §6 test-debt ALL DONE — (1) mypy extended to client/ (38 files clean, pyproject.toml+Makefile updated); (2) 23 collector golden-fixture tests (test_collectors_parsers.py — CIM int→float coercions, as_list() quirk, serial hashing, chassis inference, cert timestamps); (3) 8 locale-fixture tests (test_locale_fixtures.py — Cyrillic hostname/OS-caption/disk-name passthrough, English level invariant, Unicode truncation); (4) 34 scoring boundary tests (test_scoring_boundaries.py — all RSI/perf/wear/risk thresholds + clamping + monotonicity + determinism). Gate green: ruff/mypy/bandit clean, 486 tests, cov 93.46%. NEXT = next item from cctodo.md (§7 or remaining §6 operator-UX items).** W4.3 summary: D6 — KP41 demoted to conditional enhancer (anchor required: throttle<85% OR dirty_shutdown>0; weight min(kp*0.3, 1.0) was min(kp*0.6, 2.5)); WHEA removed from power_thermal+memory (firmware/ASPM noise, specificity≈0). D5 — compute_risk() accepts domain_values from W4.2 engines as supplementary log-odds via _domain_lo() (storage/battery/os_degradation/disk_fill consumed; trajectory/fleet_anomaly not yet wired). Scale — overall now 0..100 matching risk_exposure and all W4.2 axes; top class for degrading fixture is now stability (was power_thermal = D6 defect). Security hardening from review: _domain_lo() guards NaN/Inf (→0.0 = UNKNOWN) + clamps [0,100]; bare dict subscripts replaced with .get()+truthy guard; _has_anchor uses explicit ds>0. Tests: 11 new/updated Bayesian tests covering D6 enforcement, domain supplement, None=UNKNOWN neutrality.
- **W4.2 fleet-anomaly engine (5th/last of 5 domain engines) — MERGED `0b1d4de` (feat `3978a10`, branch `feat/w4.2-fleet-anomaly-engine`, gate green: ruff/mypy/bandit clean, cov 93.33%, smoke OK, subagent-reviewed HIGH+MEDIUM fixes applied):** new pure `server/analytics/fleet_anomaly.py` → `compute_fleet_anomaly_risk(cohort_stats, *, device_trust)` = **`fleet_anomaly_risk`** Score100 axis (higher_is_worse). Two fleet-event patterns: (1) **bad OS patch / driver rollout** — elevated BSOD rate or RSI degradation across the model cohort; (2) **site-wide KP41 cluster** — elevated kernel_power_41 across site devices (building power, not PSU failures). Cohort key = `model` (devices table); site key = `site_code`. Minimum ≥ 2 devices → verdict; `cohort_size < 2` → UNKNOWN. Confidence ≤ medium (small-fleet statistics). `cohort_kp41_pct` stored in lineage only (low specificity without site context). Two permanent blind spots: os_build not used as cohort sub-key (in JSON payload, not a direct column); correlation ≠ causation. New `db.get_fleet_cohort_stats()` + `db.get_device_model_site()` (both from devices table). Wired once in `recompute_scores`; surfaced via `/diagnostics/{id}` + «Аномалии флота» device block. 24 tests. Review fixes: dual-source `model` mismatch eliminated (always from devices table); distinct UNKNOWN reason for `cohort_size=0` vs `cohort_size=1`; confidence-assertion test tightened; reason populated for positive-score path.
- **W4.2 storage-health engine (1st of 5 domain engines) — MERGED `e52ff83` (feat `cfb36f3`, branch `feat/w4.2-storage-engine`, gate green cov 92.71%, smoke OK, subagent-reviewed APPROVE-WITH-FIXES applied):** new pure `server/analytics/storage.py` → `compute_storage_risk(hist, hb, *, device_trust)` = **`storage_risk`** Score100 axis (higher_is_worse). SMART-led (reallocated sectors / cumulative R+W errors strong; SSD wear% + temp graded; power-on-hours context); **disk latency = confirmation ONLY** (guarded `if value>0`: amplifies a SMART signal, never raises risk alone — kills Defender/OneDrive/RAM/thermal false alarms). Worst disk wins; same gating (untrusted→withheld; no SMART on any disk→UNKNOWN, never confident-0). Current-state verdict (wear *trend*/ETA stays in W4.1 trajectory). Wired once in `recompute_scores`, surfaced via `/diagnostics/{id}` (+`storage_risk`) and «Здоровье диска (SMART)» device block. 14 unit + 1 integration test. Review fixes applied: latency-invariant test tightened to `==0.0`; `disks_total` added to lineage. **Accepted-minor (MEDIUM):** absent/non-numeric I/O counter → silent 0 (conservative: under-counts, never over-counts; prod payloads are pydantic `int|None` so non-numeric can't reach it). Day-1 lumped `wear` left unchanged (additive — multi-engine, not unified core).
- **W4.2 battery-health engine (2nd of 5 domain engines) — MERGED `cf2170f` (feat `0ea44dc`, branch `feat/w4.2-battery-engine`, gate green: ruff/mypy clean, 177 tests, battery.py 94% cov, smoke OK, subagent-reviewed APPROVE):** new pure `server/analytics/battery.py` → `compute_battery_risk(hist, *, device_trust)` = **`battery_risk`** Score100 axis (higher_is_worse). Capacity fade leads (design vs full mWh → wear%, agent-reported or re-derived & **rounded to 1dp to match the agent's own formula** so derived/reported never straddle a grading boundary); `cycle_count` = **context only** (≤+12, never alone pushes «bad»). Honest blind spot: WMI can't see swelling/heat/charge-habits → confidence **capped at medium** even on a pristine battery, `missing_evidence` ALWAYS carries the swelling caveat (healthy capacity ≠ safety — «swelling≠age»; memory `[[battery-swelling-not-age]]`). Same gating: untrusted→withheld; **no battery (desktop)→not-applicable** (flagged `source_lineage.battery_present=False`, distinct from a blind spot); present-but-no-metric→UNKNOWN (never confident-0). Current-state verdict; wear *trend*/ETA stays in W4.1 trajectory (`trends.py:_battery_wear`, not duplicated). Wired once in `recompute_scores`; surfaced via `/diagnostics/{id}` (+`battery_risk`) and «Здоровье батареи» device block (renders N/A vs UNKNOWN distinctly + shows the swelling blind spot to the operator). 15 unit + 1 integration test. **Accepted/deferred (LOW, pre-existing):** dashboard chip `risk_color` (good<25/warn<50/bad≥50) vs stored `band_for_risk_score` (good<15/watch<40/bad≥40) thresholds diverge — shared by storage+trajectory blocks too, parked for a single global follow-up. Reviewer praised surfacing `missing_evidence` on the dashboard → suggest backporting to the storage block.
- **W4.2 OS-degradation engine (4th of 5 domain engines) — MERGED `d610883` (feat `e18362d`, branch `feat/w4.2-os-degradation-engine`, gate green: ruff/mypy/bandit clean, cov 93.32%, smoke OK, subagent-reviewed HIGH-fixes applied):** new pure `server/analytics/os_degradation.py` → `compute_os_degradation_risk(historical, *, device_trust)` = **`os_degradation_risk`** Score100 axis (higher_is_worse). **Leading signal = RSI** (Win32_ReliabilityStabilityMetrics, 1–10 scale lower=worse; Windows synthesises crashes/OS-failures/app-failures internally — clearest single OS-health proxy). Bands: <2.0→+60 (critical), <3.5→+40 (severe), <5.0→+22 (high), <7.0→+10 (watch), ≥7.0→0 (stable); boundaries exclusive (`<`). **Confirmation = `bugchecks_30d`** (BSODs, strongest evidence ≥3→+30; ≥1→+15) + `dirty_shutdowns_30d` (event 6008 ≥5→+12; ≥2→+6). **Boot-rot independent**: `avg_boot_ms` ≥120s→+12; ≥60s→+5 (current-state level; slope owned by W4.1 trajectory). **KP41 excluded** (D6: specificity≈0, power/transient noise). `app_crashes_30d` = lineage only, never drives score (app-specific, not OS-level). **Confidence caps at medium** (RSI opaque heuristic; crash counts can't attribute cause). **Pending-reboot permanently in `missing_evidence`** (not collected by any collector). Same gating: untrusted→withheld; no RSI and no crash counts→UNKNOWN (never confident-zero). Wired once in `recompute_scores`; surfaced via `/diagnostics/{id}` (+`os_degradation_risk`) and «Стабильность ОС» device block. 31 tests (boundary semantics, app-crash non-inflation, dead-assert fixed). Review HIGH-fixes: `_hist()` immutable merge (`{**base, **kw}`); dead `if s.value is not None` → `assert s.value is None` + separate inflation test; RSI boundary semantics documented + boundary test added.
- **W4.2 disk-fill/servicing-collapse engine (3rd of 5 domain engines) — MERGED `a0aa69b` (feat `271d9f0`, branch `feat/w4.2-disk-fill-engine`, gate green: ruff/mypy/bandit clean, cov 93.01% / engine 93%, smoke OK, subagent-reviewed APPROVE [code] + APPROVE [security]):** new pure `server/analytics/disk_fill.py` → `compute_disk_fill_risk(hb_series, events, *, device_trust)` = **`disk_fill_risk`** Score100 axis (higher_is_worse). **Leading = free-space level graded on the MEDIAN of the recent heartbeat window** — the median IS the rebound guard: a one-off Windows-Update cleanup dip can't move it (not alarmed) but a persistently-full drive does (alarmed); a fresh dip is surfaced (not hidden) as a possible transient in `missing_evidence`. **Window bounded to last 14 days by `received_at`** (review fix — stale months-old lows must not fabricate an alarm; depletion *history* = trajectory's job). Bands: <3%→75, <8%→55, <15%→30, <20%→18. **Confirmation = WindowsUpdateClient failures (ids 20/25/31, matched by PROVIDER not bare id):** low disk → amplify (+20); healthy disk + ≥3 failures → moderate standalone «not patching» (+28, watch band, medium conf, cause-uncertain); <3 = routine/ignored. Same gating (untrusted→withheld; no free-space data ∧ no collapse→UNKNOWN; **healthy directly-measured drive→confident-0 high conf** — unlike battery, free space has no blind spot). Current-state verdict; slope/ETA stays in W4.1 trajectory (not duplicated). **Agent:** `client/collectors/events.py` whitelist gains WU failures (pure stdlib/PS, additive, narrow) = data source for the servicing half. New `db.get_recent_events()` (parameterized, metadata only, no message body). Wired once in `recompute_scores`; surfaced via `/diagnostics/{id}` (+`disk_fill_risk`) and «Заполнение диска / обслуживание Windows» device block (renders UNKNOWN vs healthy distinctly + shows blind spots/transient note). 21 unit + 1 integration test. Decision why → memory `[[disk-fill-median-rebound-guard]]`. **Accepted/deferred (LOW, pre-existing):** dashboard `risk_color` vs `band_for_risk_score` threshold divergence (shared across all engine blocks — single global follow-up). Security review: thin-agent intact, SQL parameterized, autoescape on (event `source` only counted, never rendered), no PII regression.
- **W4.0 (LEAN) — skip pointless event-rescore — MERGED `7e64386` (feat `4509b7a`, branch `feat/w4.0-drop-event-rescore`, gate green cov 92.03%, smoke OK, subagent-reviewed APPROVE no-findings):** `events` ingest no longer calls `recompute_scores` (`scores = None if msg_type=="events" else recompute_scores(did)`). Verified safe: scoring reads only latest inv/hist/hb — never the events table (grep: nothing in `server/scoring/` touches events); agent ignores the ingest response body (status only); dashboard reads scores from DB. Saves the now-O(n²) W4.1 Theil-Sen pass on every events message. Events response → `scores:null`/`scores_updated:false`; scores still served from DB + recomputed on inv/hist/hb (`touch_device`/`store_events`/`evaluate_trust` untouched). 1 spy test `test_events_on_scored_device_do_not_rescore` (RED→GREEN, proves recompute NOT called). **Owner decision (2026-06-07): LEAN scope — drop event-rescore ONLY; the full async rescore queue is DEFERRED (NOT pending) until a measured latency/scale need — YAGNI + SQLite single-writer means a worker thread adds no write throughput at PC-fleet scale; revisit when W4.2 cohort analytics actually needs cross-device batching.** Communicate with this user in simple Russian, schoolchild level `[[communicate-simple-russian]]`.
- **W4.1 deterministic trend engine — MERGED `739e95b` (feat `ca16e3b`, branch `feat/w4.1-trend-engine`, gate green cov 92.13%, smoke OK, subagent-reviewed APPROVE-WITH-FIXES, both MEDIUM fixed):** new pure `server/analytics/` (`trends.py` engine + `diagnostics.py` read-side). Robust **Theil-Sen** slope + **ETA-to-threshold** over **server-time (`received_at`)** series for depletion/degradation domains: SSD wear→100%, system free-space→0%, battery wear, boot-time + CPU-throttle (direction-only). Pure arithmetic (D4, no ML). New **`trajectory_risk`** Score100 axis reuses W0.5 envelope + gating (untrusted→withheld; <3 pts or stable/improving→UNKNOWN, never a fabricated ETA/confident-zero). Review fixes: a part **already at/past its boundary while still worsening** reads imminent (ETA 0, not dropped); a **drive/battery replacement** (monotonic counter reset >10 units opposite) **anchors** the slope to the new part. Computed once in `recompute_scores` (single source of truth), surfaced via `GET /api/v1/diagnostics/{id}` + «Траектория» block on device page. 22 tests `test_analytics_trends.py`. Also fixed `client._machine_guid()` to fall back to uuid when `winreg` absent off-Windows (matched its own comment; unblocked the suite gate).
- _(prior P0 context below)_ **✅ ALL P0 CLOSED + REVIEWED — W0.5 Score100 merged `341bbdb`, post-merge reviewed+hardened `bd8a61d` (both HIGH were false-positive; gating sound). `main`↔origin in sync. W4 analytics unblocked.** User (2026-06-03) directed the full **diagnostic-center vision** = W4: `server/analytics/` (robust→trajectory→cohort→hypotheses→queue) + **8 Score100 axes** (each `value/direction/band/confidence/trend/factors/missing_evidence/source_lineage`: performance/reliability/wear/risk_exposure/observability/trajectory_risk/fleet_anomaly/operator_urgency) + action-queue dashboard + `/api/v1/diagnostics`. **Principal-eng sequencing (honors cctodo §8 P0-gate — analytics distrusted until P0 closed):** W0.2 → **W0.5** (Score100 envelope = confidence-gating, load-bearing for the spec's `confidence/missing_evidence/UNKNOWN-ranked`) → W0.4 → W4. NEXT = W0.5. P1/W1 notes below are prior context.
- W0.3 COMPLETE. Phase W1: **W1.0 `.claudeignore` ✅** (`b2ffd29`); **W1.1 site/org identity ✅** (`440d447`); **W1.2 cert-expiry ✅ merged** (`cb9555a`: cert metadata in `historical.certificates`, `days_until`, dashboard highlight; security-reviewed APPROVE).
- **W1.3 live dashboard** on `feat/live-dashboard`: **3a backend ✅** (`c78eaab`: enriched `GET /api/v1/devices` [device_trust, unknown_domains, regressed_count, stale+last_seen_age_sec, cert_min_days/cert_expiring, ack] + `acknowledgements` table + `POST /devices/{id}/ack`; gate 92%).
- **W1.3b live dashboard ✅** (`a19fd33`: `_fleet_body.html` partial + `/fleet/fragment`; `fleet.html` shell polls/swaps ~12s; KPI-filter cards, site grouping, search, per-device alert badges, ack-from-list; gate 91%). In **security+quality review** of `feat/live-dashboard` → reviewed APPROVE (4 minor fixes applied: immutable context, ack-note cap, fmt_age guard, live subtitle) and **merged to `main` @ `d77e934`**.
- **PHASE W1 COMPLETE** — W1.0–W1.3 all on `main`, gate green (coverage 90.85%). **P1 deployability — doing ONLY the next item this turn: ingest auth** on `feat/ingest-auth`. Optional shared token (`ingest_token`; empty = disabled, non-breaking): server checks header on `/api/v1/ingest`, agent sends it. **ingest auth ✅ merged** (`b49a738`): server checks `X-SRP-Token` constant-time (`hmac.compare_digest`), agent sends it; empty token = off (non-breaking); security-reviewed APPROVE. **close public-IP default ✅ merged** (`538c7ad`): removed `_DEFAULT_SERVER_URL` public IP → `ClientConfig.server_url=""`; new `ConfigError`+`validate_runtime_config` (raises if empty/whitespace); `agent.main` validates AFTER `--server` override → `SystemExit(2)` on miss (fail-closed); committed `client/config.json` template emptied; README+CHANGELOG updated; `tests/test_config.py` (9 tests incl. exit-path + `--server` rescue). Operator sets `server_url` at install (LAN typical; public IP still a valid explicit choice). Gates green (cov 90.91%, smoke OK); security-reviewed APPROVE-WITH-FIXES → MEDIUM fixed (literal prod IP removed from error string; README keeps it per product req). Accepted-minor: `Transport.__init__` builds `_ingest_url` from unvalidated `server_url` (pre-existing; production path gated by `main`, only direct/test construction affected). **Windows service under LocalSystem ✅ merged** (`1145fb8`): chose **Task Scheduler** (native, zero extra binaries — pure-stdlib loop can't be a real SCM service, pywin32 banned). `client/deploy/install-service.ps1`+`uninstall-service.ps1` (AtStartup, SYSTEM, RunLevel Highest, restart 3×1min, no time limit; install merges `server_url`/site/token→config.json UTF-8 **no-BOM**, validates via one `--once` pass [no `--server`, tests installed path], registers+starts). `agent.py`: `setup_logging` + opt-in stdlib `RotatingFileHandler` (1MB×3) via `--log-file`; startup line logs `user=` (confirm SYSTEM); `server_url` redacted (strip `user:pass@`) before logging. **WHY SYSTEM:** unblocks SMART/StorageReliabilityCounter/WMI collectors (else telemetry-trust → UNKNOWN). README deployment section folds **TLS reverse-proxy note (P1 #2 doc ✅)** + fixed stale "ingest unauthenticated" note. `tests/test_agent_logging.py` (5: logging + URL redaction). Gates green (cov 90.91%, smoke OK); PS scripts AST-parse-clean (Windows-only, not CI-gated); security-reviewed APPROVE-WITH-FIXES → H1(validation `--server` drop)/H3(url redaction)/M4(isinstance)/M5(uninstall warn) applied; H2 verified-safe (COM `-Execute`); M1/M2/M3 non-blocking. design via /grill-me (6 decisions). REMAINING P1 (separate, NOT done): signed config/schema (optional), transport reconnect-jitter + idempotency dedup, server body-size + per-device rate-limit. Accepted-minor: stale `# nosec B105` on empty-token defaults (harmless). Pending: CLAUDE.md "Pasted text #1" re-paste; `CONTINUITY.md` now tracked in git. CLAUDE.md rewritten token-optimized (≤80 lines: codegraph daemon · model routing · subagent-by-default · memory tiers · gates).

### Next — Phase W1 (scalable remote monitoring)
- **W1.1 Site/org identity:** `ClientConfig.site_code` (+ optional `site_name`), manually assigned per deployment; `Envelope.site_code/site_name` (additive, forward-compat); `devices` table cols (COALESCE on touch so heartbeat doesn't wipe); fleet grouped/filterable by site. Grouping only — NOT auth/tenancy isolation (defer to prod P1).
- **W1.2 Certificate-expiry:** new agent PowerShell collector (`Cert:\LocalMachine\My` + `CurrentUser\My`) → subject/issuer/thumbprint/not_after/not_before; folded into `historical` payload (`certificates: list[CertInfo]`) — no new msg_type; `certificates` source_health (no bayesian class); server derives days-to-expiry; dashboard highlights soon-expiring (<30d red). NEVER read private keys (stdlib agent unchanged otherwise).
- **W1.3 Live dashboard + feedback:** vanilla-JS poll of `/api/v1/devices` (~10–15s) — no SPA/websocket; site-grouped + search/filter; KPIs (at-risk / UNKNOWN / expiring-certs / regressed / stale); staleness flag (agent silent > N×interval); operator feedback = per-device ack + note (new endpoint + table) — NOT remote agent control (scope ceiling). Use `frontend-design`.
- Then broader `cctodo.md` P1 (ingest auth/TLS, Windows service) when deploying.
- Pending: CLAUDE.md "Pasted text #1" re-paste (never arrived).
- Accepted-minor: `_num` bool→1.0 coercion; `partial` dual-meaning; storage validates first disk only (v1).

## Open questions (UNCONFIRMED if needed)
- `UNCONFIRMED`: CLAUDE.md "Pasted text #1" (~9 lines) never arrived — need re-paste before applying.

## Working set (files/ids/commands)
- Repo: `github.com/SergioTheFirst/srp.git`, `main` @ `dff4e51`.
- Trust code: `server/trust/*`, `server/pipeline.py` (`evaluate_trust`/`recompute_scores`), `server/db.py` (trust tables), `server/web/templates/device.html`, `shared/schema.py`, `client/collectors/*`.
- Commands: `make check`; `python -m pytest -q`; git branch→commit→`merge --no-ff`→`push origin main`.
- Untracked (by design): `.claudeignore`, `CONTINUITY.md`.
