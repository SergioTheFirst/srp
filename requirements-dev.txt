# Dev / test dependencies. Installs the runtime set plus the QA toolchain.
-r requirements.txt

# Testing
pytest>=8.0
pytest-cov>=5.0    # coverage report (pytest --cov=server --cov=shared)
httpx>=0.27        # required by fastapi.testclient.TestClient

# Quality gates (configured in pyproject.toml; run via the Makefile / CI)
ruff>=0.5          # linter + formatter (style, imports, likely bugs)
mypy>=1.10         # static type checking (shared + server)
bandit>=1.7        # static security analysis
pre-commit>=3.7    # local git hooks mirroring the CI checks (optional)
