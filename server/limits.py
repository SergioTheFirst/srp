"""Границы сборки и демо-режима.

MAX_DEVICES — сколько компьютеров принимает /ingest; None = без ограничения.
Ограничения нет ни в приватной сборке, ни в публичной: лимит в 3 машины
существовал ровно для того, чтобы продавать «полную версию», а продажи сняты
(2026-09-08) — проект развивается на добровольные взносы. Поле оставлено:
оно нужно демо-стенду и может понадобиться тому, кто разворачивает SRP у себя.
"""

import os
from typing import Optional

MAX_DEVICES: Optional[int] = None

DEMO_MODE: bool = os.environ.get("SRP_DEMO", "") == "1"

DEMO_READONLY_DETAIL = "демо-режим: только чтение"


def limit_detail(n: int) -> str:
    return f"бесплатная версия: до {n} компьютеров — новый device_id отклонён"
