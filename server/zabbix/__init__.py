"""Экспорт предиктивного вердикта SRP во внешний Zabbix по протоколу траппера.

Подсистема выключена, пока в ``server/config.json`` не задан адрес
(``zabbix.server``). Ни одной сторонней зависимости: протокол траппера — это
``socket`` + ``struct`` + ``json``.
"""
